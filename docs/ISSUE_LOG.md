# Issue Log

## 2026-09-03 — 네이버 이관 예약의 관리자 취소 연결 누락

- 상태: 공통 원인 수정과 운영 배포를 완료했다. 기능 커밋 `6bec5bb`을 `main`에 푸시한 뒤 Cafe24 API와 Ubuntu 미니PC 감시기에 반영했다. 대상 원장 `#8318`은 사용자가 관리자 패널에서 취소하기 전까지 `confirmed`로 보존했고 고객취소 작업도 0건이다.
- 증상 → 판정 → 부작용 → 결과: 2026-09-05 A홀 11:00-14:00 예약의 관리자 취소 요청이 HTTP 400으로 끝났다. 패널은 미래 네이버 예약이므로 취소 버튼을 표시했지만, API는 `confirmed_email_event_id > 0`만 원본 확정의 증거로 인정해 작업을 만들기 전에 거절했다. 따라서 네이버 취소·스페이스클라우드 복제본 삭제·원장 변경·문자 발송은 모두 0건이었고 실제 네이버 예약과 스페이스클라우드 복제본은 남아 있었다.
- 기존 보호 목적과 이력: 커밋 `0927964`가 도입한 관리자 고객취소 경로는 다른 고객 예약의 오취소를 막기 위해 원장·확정메일·예약번호·홀·날짜·시간을 취소 직전 다시 잠그고, 외부 화면의 정확한 예약 한 건을 확인한 뒤 한 번만 클릭한다. 커밋 `4e5fc5c`와 `6afae71`은 확정메일 작업이 없는 플랫폼 내보내기 예약의 취소메일 처리에서 예약번호·예약자명과 인증된 실제 후보를 요구했다. 이 실패 폐쇄 목적과 제출 체크포인트는 그대로 유지했다.
- 기존 구현 판정: `일부 있음`이었다. 기존 고객취소 큐와 Saga, 작업의 `booking_ledger_id`, 네이버 실제 예약 API·취소 패널 검증, 스페이스클라우드의 taskless 이관 복제본 검증과 삭제 후 연속 부재 확인을 재사용했다. 새 테이블·필드·큐·작업 종류·상태값은 만들지 않았다. 관리자 접수와 후속 삭제가 이메일 ID만으로 원장을 연결하던 정확한 두 빈틈만 기존 원장 ID와 이관 신원 판정에 연결했다.
- 근본 원인과 개선: 대상은 7월 네이버 내보내기로 이관돼 확정메일 ID가 원래 없었다. 초기 이관기는 내보내기 행의 명시적 `확정` 상태보다 오래된 `active=false` 값을 우선해 확정 원본을 `cancel_payload_json`에 잘못 저장했고, 이후 상태 보정은 원장을 `confirmed`로만 복구해 원본 위치는 고치지 않았다. 이제 API는 `naver/platform-export`, 취소·자동취소 세대 없음, 예약번호·날짜·시작·종료·홀·예약자명이 모두 일치하는 명시적 확정 내보내기 원본만 별도 신원 유형으로 인정한다. 과거 오분류 원본은 취소 접수 트랜잭션에서 `payload_json`으로 복구하고 기존 증거도 보존하며, 작업은 이메일 ID 대신 기존 `booking_ledger_id`로 연결한다.
- 유지한 불변조건: 이메일 기반 신규 예약은 기존 확정메일 ID 검사를 계속 사용한다. 이관 예약도 원본 출처·원장·예약번호·예약자명·상품·홀·날짜·시간 중 하나라도 달라지면 취소하지 않는다. 네이버 취소 직전에는 실제 API와 취소 패널을 다시 확인하고, 스페이스클라우드 복제본은 인증 API의 유일 후보와 빈 taskId·예약번호·예약자명이 맞을 때만 삭제한다. 클릭 직전 DB 재잠금, 한 번 제출, 삭제 후 두 번 연속 부재 확인과 중복 요청 멱등성도 유지했다.
- 검증 범위와 결과: PHP 문법·자체검사, 운영 스키마를 임시 테이블로 가린 MySQL 접수/중복요청/원본복구 검사, NOW-mode 자체검사, 네이버·스페이스클라우드 검증 72개, 충돌 정책과 문자 Outbox 검사를 통과했다. 운영 대상은 새 판정기로 `platform-export` 신원과 접수 시 원본복구 필요 상태가 확인됐고, 실제플랫폼 감사도 네이버 원본과 스페이스클라우드 복제본을 `ok`로 판정했다. 사용자가 직접 취소할 예약이므로 실제 취소 클릭과 원장 변경은 검증에서 제외했다.
- 배포 확인: Cafe24 설치본·release의 API 해시는 로컬과 `1e264490f68d2b41330ac230051694b6d6e9fa3fd8a67189e180b87a2b25c5f8`로 일치한다. 미니PC 감시기·업로더 해시는 각각 `55f240a89488f1f0f4b33a170881cdc8703f5bac9fa68ed70dfb7367ff7ffc65`, `13a1395c2ab621d744eb0539c18c85f53436f9702d00ed47550a36d525a5635f`이며 직전 파일은 `/home/kiosk-j/rhythmjoy-deploy-backups/6bec5bb-20260903/previous/tools`에 보존했다. HTTP·메일 수집·공개 캐시·미니PC 감시기는 모두 `active`, 감시기 `NRestarts=0`이다.
- 관련 파일: `www/sync-admin/api.php`, `tools/spacecloud-watch.mjs`, `tools/spacecloud-playwright-uploader.mjs`, `ops/rhythmjoy_sync_admin_idempotency_selftest.php`.

## 2026-09-01 — 네이버 다주차 이동 화살표의 SPA 전환 누락

- 상태: 공통 원인 수정, 운영 배포, 실패 작업 복구, 실제 플랫폼·문자·관리자 알림 재검증까지 완료했다. 기능 커밋 `a329471`을 `main`에 푸시한 뒤 Cafe24와 Ubuntu 미니PC에 배포했다.
- 증상 → 판정 → 부작용 → 결과: 스페이스클라우드 원천 예약의 네이버 차단 작업 `#912`가 달력 준비 중 `page.waitForFunction: Timeout 10000ms exceeded`로 6회 종료돼 `needs_review`가 됐고 확정문자 `#52080`은 플랫폼 완료를 기다리며 `pending`이었다. 실패 위치는 첫 슬롯을 읽기 전의 `gotoNaverWeekContainingDate`였으며 `beforeSlots`·`appliedSlots`, Saga 무장·완료 시각, 문자 제공사 응답이 모두 비어 있어 네이버 변경과 문자 발송은 각각 0건이었다. 원장 `#10080`과 이메일 이벤트 `#945`는 같은 확정 세대를 유지했다.
- 기존 보호 목적과 이력: 커밋 `c0bdc5ff`의 주간 화살표 이동은 클릭 뒤 고정 1초를 기다렸고, 커밋 `431d57f`가 이를 화면 기간 문구가 실제 바뀌는 상태 대기로 교체했다. 이전 전환이 불확실할 때 다음 화살표를 누르지 않아 목표 주를 지나치거나 다른 주를 변경하지 않는 안전 목적은 정상적이므로 유지했다.
- 기존 구현 판정: `일부 있음`이었다. 기존 `gotoNaverWeekContainingDate`, 네이버 날짜 선택기, 주간 기간 파서, `prepareCalendar`의 홀·날짜·시간 슬롯 준비 검사와 관련 테스트가 이미 같은 책임을 소유했다. 새 테이블·필드·큐·작업 종류·상태값은 만들지 않았고, 불안정한 반복 주간 화살표를 기존 날짜 선택기에 정확히 연결하는 빈틈만 수정했다.
- 근본 원인과 개선: 운영 세션에서 주간 다음 버튼의 첫 클릭은 기간 문구와 예약상태 API 응답을 바꿨지만, SPA 내부 전환이 끝나기 전에 이어진 다음 클릭은 네이버가 요청 자체를 만들지 않고 삼켰다. 기간 문구 변경이나 API 200만 기다리는 방식으로는 다음 조작 가능 상태를 증명할 수 없었고, 재시도나 제한시간 연장은 같은 불확실성을 반복한다. 이제 날짜 선택기를 한 번 열어 목표 월을 화면의 실제 월 변화로 확인하고, 그 월의 전체 날짜 버튼 수를 검증한 뒤 목표 일자를 순번으로 한 번 선택해 적용한다. 적용 뒤 정확한 일요일~토요일 기간이 보일 때만 진행하고, 이어서 기존의 정확한 요일·시간·상태 판독기가 슬롯을 다시 확인한다. 고정 시간 경과로 성공을 판단하는 코드는 이 경로에 없으며 제한시간은 상태가 끝내 확인되지 않을 때 클릭 없이 중단하는 실패 상한으로만 남겼다.
- 유지한 불변조건: 목표 주가 이미 보이면 날짜 선택기를 열지 않는다. 월 이동이 확인되지 않으면 다음 월 버튼을 누르지 않고, 목표 월·일이 하나로 확정되지 않으면 적용하지 않으며, 적용 결과가 불확실하면 같은 실행에서 재적용하지 않는다. 실제 슬롯이 `available`일 때만 차단하고 각 변경 뒤 `suspended`를 다시 읽는다. 문자도 작업 `done`과 확정 원장 세대를 DB에서 확인한 기존 Outbox가 한 번만 발송한다.
- 운영 복구: 배포 전 DB 백업 `/home/clown313python/rhythmjoy_ops/backups/db/raj-20260901-173637.sql.gz`과 미니PC 직전 두 파일 `/home/kiosk-j/rhythmjoy-deploy-backups/a329471/`을 보존했다. 작업·원장·이벤트의 스페이스클라우드 전용 세대 ID, 홀·날짜·시간, 확정/취소 상태, 유일한 동세대 작업과 문자 Outbox를 트랜잭션에서 다시 잠그고 외부 변경·문자 시도가 모두 0건일 때만 같은 작업을 재대기했다. 재실행은 네 시간 모두 `available → suspended`로 각각 한 번 변경해 `done/blocked`로 끝났고 문자는 Aligo 성공 코드로 1회 발송됐다.
- 검증 범위와 결과: 네이버 인증·날짜 이동·슬롯·변경 안전경계 38개, 취소/문자 Saga 경계 12개, NOW-mode 자체검사, Node 문법·diff 검사를 통과했다. 배포 전 실제 운영 세션에서 수정 후보가 같은 네 슬롯을 모두 `available`로 읽었고, 배포·복구 뒤 독립 고객 실제플랫폼 감사는 스페이스클라우드 원본 확정과 네이버 네 칸 전체 `예약불가`를 `ok`로 판정했다. 반영 감사는 633/633 정상이고 문제·중복·공개일정 불일치·수집 누락은 모두 0건이다. 관리자 알림은 중요·활성·미확인 모두 0건이며 HTTP·Apache·메일 수집·공개 캐시·감사 타이머·미니PC 감시기는 모두 `active`, 감시기 `NRestarts=0`이다. 스페이스클라우드 등록/삭제와 네이버 취소 경로는 변경하지 않아 실제 외부 쓰기를 재연하지 않았고, 인접 실패·재시도·문자 경계는 기존 Saga 및 NOW-mode 검사로 한정했다.
- 관련 파일: `tools/naver-playwright-availability.mjs`, `tools/naver-playwright-availability-test.mjs`.

## 2026-08-31 — 네이버 주간 달력 날짜 셀 렌더 경합

- 상태: 공통 원인 수정, 운영 배포, 실패 작업 복구, 실제 플랫폼·문자·관리자 알림 재검증까지 완료했다. 기능 커밋 `bcbc5c9`를 `main`에 푸시한 뒤 Cafe24와 Ubuntu 미니PC에 배포했다.
- 증상 → 판정 → 부작용 → 결과: 스페이스클라우드 원천 예약의 네이버 차단 작업 `#895`가 첫 시간의 `day-cell-not-found`, `cellCount=0`을 실제 슬롯 부재로 판정해 `needs_review`가 됐고 확정문자 `#52050`은 플랫폼 완료를 기다리며 `pending`이었다. 작업의 `appliedSlots`는 비어 있고 네이버 변경·문자 제공사 호출·Saga 무장 흔적은 모두 0건이었으며, 원장 `#10063`과 이메일 이벤트 `#926`은 동일한 확정 세대를 유지했다.
- 기존 보호 목적과 이력: 커밋 `47483107`은 고정 700ms 대기를 없애고 요청 시간 행이 실제 보일 때까지 기다리도록 바꿨으며, 커밋 `2dd713c6`은 알 수 없거나 예상 밖인 슬롯을 클릭하지 않고 `needs_review`로 닫았다. 렌더 속도가 아니라 화면 상태로 진행하고, 불확실하면 외부 변경 전에 중단하는 두 목적은 모두 유지했다.
- 기존 구현 판정: `있음`이었다. `scrollNaverCalendarToHour`의 상태 대기와 `readNaverWeeklySlotDom`의 날짜 셀·상태 판독을 그대로 재사용했다. 새 함수·타이머·테이블·필드·큐·작업 종류·상태값은 만들지 않았고, 두 기존 구현 사이에서 요청 날짜 셀의 준비 완료를 확인하지 않던 연결 한 곳만 보완했다.
- 근본 원인과 개선: 네이버의 가상 주간 달력은 스크롤 뒤 시간 행의 박스가 먼저 보이고 그 안의 7개 날짜 셀과 상태 버튼이 다음 렌더 단계에서 생길 수 있다. 기존 준비 함수는 행 표시만 확인한 직후 슬롯을 읽어 이 짧은 중간 상태를 실제 `not_found`로 저장했다. 이제 요청 요일의 셀 안에 `예약가능`·`예약불가`·`확정`·매진 중 하나로 분류 가능한 버튼이 실제 화면에 보일 때만 준비 완료로 진행한다. 제한시간은 영원히 멈추지 않게 하는 실패 상한일 뿐 성공을 결정하는 타이머가 아니며, 상한 안에 상태가 나타나지 않으면 기존처럼 클릭 없이 재시도/확인 필요로 닫힌다.
- 유지한 불변조건: 대상 홀·주·날짜·시간과 상태 버튼이 하나로 식별되기 전에는 클릭하지 않는다. 변경 뒤에는 요청한 최종 상태가 실제로 보일 때까지 기다린 다음 같은 DOM 판독기로 다시 읽으며, 다른 상태·로그인 실패·모호한 화면은 성공으로 승격하지 않는다. 문자도 작업 결과가 DB에 `done`으로 커밋되고 원장 세대가 여전히 확정인 경우에만 기존 Outbox가 한 번 발송한다.
- 운영 복구: 배포 전 DB 백업 `/home/clown313python/rhythmjoy_ops/backups/db/raj-20260831-164000.sql.gz`과 미니PC 직전 파일 `/home/kiosk-j/Rhythmjoy_calendar/deploy-backups/bcbc5c9-20260831-1641/previous/tools`를 보존했다. 공용 예약번호가 비어 있는 이 경로의 기존 설계에 따라 스페이스클라우드 전용 예약 ID, 정규 원장 키, 작업 dedupe 키, 이벤트 `#926`, 원장 `#10063`, 홀·날짜·시간·예약자·상품이 모두 일치하고 다른 미완료 차단 세대가 없을 때만 같은 작업을 재대기했다. 재실행은 두 시간 모두 `available → suspended`로 각각 한 번 변경하고 사후 검증해 `done/blocked`로 끝났으며, 대기 문자도 Aligo 성공 코드로 1회만 발송됐다.
- 검증 범위와 결과: 네이버 렌더·인증·변경 안전경계 37개, 취소/문자 Saga 경계 12개, NOW-mode 자체검사, Node 문법·diff 검사를 통과했고, 배포된 수정본으로 같은 운영 세션·같은 슬롯을 읽어 준비 대기가 정상 통과함을 먼저 확인했다. 독립 고객 실제플랫폼 감사는 스페이스클라우드 원본 확정과 네이버 두 칸 전체 `예약불가`를 `ok`로 판정했고, 반영 감사는 621/621 정상·문제/중복/공개일정 불일치/수집 누락 0건이다. 관리자 알림은 중요·활성·미확인 모두 0건이고 HTTP·Apache·메일 수집·공개 캐시·감사 타이머·미니PC 감시기는 모두 `active`, 감시기 `NRestarts=0`이다. 스페이스클라우드 등록/삭제 구현은 변경하지 않아 실제 외부 쓰기를 재연하지 않았고, 그 인접 실패·재시도·문자 경계는 기존 Saga 및 NOW-mode 검사로 한정했다.
- 관련 파일: `tools/naver-playwright-availability.mjs`, `tools/naver-playwright-availability-test.mjs`.

## 2026-08-30 — 네이버 내보내기 예약 취소의 반복 격리·출처 보호식 불일치

- 상태: 공통 원인 수정, 운영 배포, 격리 작업 복구와 실제 스페이스클라우드 삭제, 독립 재검증까지 완료했다. 기능 커밋 `4e5fc5c`, 공통 출처 보호 보완 `6afae71`을 `main`에 푸시하고 Cafe24와 Ubuntu 미니PC에 배포했다.
- 증상 → 판정 → 부작용 → 결과: 네이버 취소 이벤트 `#915`는 정확한 플랫폼 내보내기 원장 `#152`가 있었지만 확정 이메일·과거 업로드 작업이 없어 `incomplete_cancellation_identity_no_side_effects`로 격리됐고 삭제 작업 `#883`도 `missing-prior-reservation` 알림을 만들었다. 첫 공통 연결 보완 후에는 스페이스클라우드 API에서 정확한 후보 1건을 찾았지만, 삭제 직전 승인에서 금액 출처가 바뀐 것을 감지해 클릭 없이 다시 `needs_review`로 중단했다. 따라서 외부 삭제나 중복 제출 없이 근본 판정식을 다시 조사했다.
- 기존 보호 목적: 확정 세대 증거 없이 같은 시간의 다른 고객 일정을 삭제하지 않는 실패 폐쇄 규칙을 유지한다. 신규 일정은 계속 `taskId + 예약번호`를 요구하고, 작업 없는 구형 일정은 원장 출처·엄격히 이전인 이벤트 순서·예약번호·홀·날짜·시간·예약자명과 로그인된 실제 후보가 모두 유일하게 맞을 때만 허용한다. 클릭 직전 DB 재잠금과 삭제 후 연속 API 부재 확인도 유지했다.
- 기존 구현 판정: `일부 있음`이었다. 기존 원장, 격리 복구 명령, 작업 큐와 상태, 구형 taskless 검증기, 최종 제출 체크포인트를 재사용했다. 새 테이블·큐·필드·작업 종류·상태값은 만들지 않았다. 기존 취소 보강 함수는 플랫폼 내보내기 원장 증명까지 연결하고, 기존에 없던 순수 신원 증명 함수는 수집·복구가 같은 판정을 공유하도록 원장 소유 모듈 한곳에만 두었다. 운영 읽기 전용 전수검사에서 같은 구조의 미래 플랫폼 내보내기 원장 49건이 확인돼 한 건 전용 예외가 아닌 공통 경로로 처리했다.
- 근본 원인: 원장 금액 보호 SQL은 `amount_source LIKE '%platform-export'` 의미로 작성돼 `platform-export`로 끝나는 값만 보호했다. 운영의 유효한 확장값 `naver-platform-export-verified`는 취소 투영 때 보호되지 않아 `naver_email`로 덮였고, 새 최종 삭제 안전검사가 이를 출처 불일치로 올바르게 차단했다. 과거 커밋 `caca07d`는 PyMySQL 퍼센트 이스케이프만 고쳤고 접미 확장값까지 포함하지 않았으며, `3881d19`가 도입한 오삭제 방지 목적 자체는 정상적이었다.
- 개선: 확정·취소·강제 재투영의 기존 금액/수수료/결제수단/원본 payload 보호식을 모두 `%platform-export%` 의미로 통일해 접두·접미 확장값도 보존한다. 취소 전 복구 판정은 금액 출처까지 계속 요구하지만, 취소 뒤 최종 삭제 승인은 변할 수 있는 금액 투영 필드 대신 기존의 불변 `source_mode`, 원본 payload 출처, 정확한 취소 이벤트·원장 세대, 자동취소 이력 부재와 실제 후보 신원으로 다시 계산한다.
- 운영 복구: 배포 전 DB 백업 `/home/clown313python/rhythmjoy_ops/backups/db/raj-20260830-014751.sql.gz`과 미니PC 직전 런타임 `/home/kiosk-j/Rhythmjoy_calendar/deploy-backups/6afae71-20260830-0148/previous`를 보존했다. 작업 `#883`이 `ready`, 클릭·무장·완료 흔적 없음, 정확한 실제 후보 1건, 다른 미완료 삭제 세대 없음일 때만 원장 출처를 복구하고 같은 작업을 한 트랜잭션으로 재대기했다. 새 감시기는 최종 잠금에서 플랫폼 내보내기 세대와 taskless 실제 후보를 승인하고 한 번 삭제했으며, 삭제 후 SpaceCloud API 2회 연속 후보 0건을 확인해 `done/finalized`로 종료했다.
- 검증과 결과: 이메일/원장 자체검사 25개, 업로더 34개, 취소 Saga 경계 12개, NOW-mode, 동일 초 이벤트 순서, Node·Python 및 내장 Python 문법 검사를 통과했다. 독립 고객 실제플랫폼 감사는 원장 `#152`를 `ok`로 확인했고, 반영 감사 610/610 정상, 수집 149건·누락 0건이다. 작업·메일·정규검사·세션·문자·관리자/고객 플랫폼 감사에서 관리자 활성 알림 후보는 모두 0건이다. 로컬·Cafe24 release·설치 importer 해시와 로컬·Cafe24 release·미니PC 감시기 해시가 일치하며, HTTP·메일 수집·공개 캐시·감사 타이머·감시기 서비스는 모두 `active`, 감시기 `NRestarts=0`이다.
- 관련 파일: `ops/rhythmjoy_email_import.py`, `ops/rhythmjoy_email_import_selftest.py`, `tools/spacecloud-watch.mjs`, `tools/spacecloud-playwright-uploader.mjs`.

## 2026-08-26 — 이메일 수집·문자 Outbox 복구 간 DB 데드락

- 상태: 대상 예약은 자동 재시도로 정상 완료됐고, 공통 원인 코드 개선과 로컬 검증을 마쳤다. 운영 배포는 아직 하지 않았다.
- 발생 성격: 이메일 수집 오류 자체는 드문 동시성 경합이며 반복적인 메일 연결·파싱 장애로 판정하지 않았다. 다만 예약 한 건이라도 같은 경고와 지연을 다시 만들 수 있어 건별 예외가 아니라 공통 잠금 순서를 개선한다.
- 증상과 결과: 2026-08-25 23:49:16 KST에 E홀 예약 이메일 이벤트 `#862`의 원장·스페이스클라우드 업로드·확정문자 Outbox handoff가 MariaDB 오류 `1213 Deadlock`으로 롤백됐다. 원본 메일을 읽음 처리하지 않는 기존 안전장치가 같은 이벤트를 23:50:09에 다시 처리했고, 원장 `#10001`, 업로드 작업 `#832`, 문자 `#51746`이 각각 `confirmed`, `done/finalized`, `sent`로 완료됐다. 문자 발송 시도는 1회이며 이메일 수집 서비스는 계속 `active`였다.
- 기존 보호 목적: 예약 작업의 `confirmation_sms_required=1`과 문자 Outbox 행을 원장 handoff 트랜잭션에 함께 기록해 예약만 반영되고 문자가 사라지는 반쪽 상태를 막는다. 감시기의 독립 복구는 누락된 문자 행을 멱등키로 복원한다. 전체 롤백, 커밋 후에만 메일 읽음 처리, 정확한 원장 세대·플랫폼 완료 확인 후에만 문자 발송하는 불변조건은 유지했다.
- 근본 원인: 감시기의 `INSERT IGNORE ... SELECT` 복구문이 문자 테이블의 AUTO_INCREMENT 잠금을 먼저 잡고 `rhythmjoy_spacecloud_tasks` 원본 행 잠금을 기다렸다. 동시에 이메일 수집기는 새 작업 행을 잠근 뒤 문자 행을 삽입하려 해 `문자 → 작업`과 `작업 → 문자`의 역순 대기가 생겼다. InnoDB 최신 데드락 기록에서 두 쿼리와 롤백된 수집기 트랜잭션을 직접 확인했다.
- 개선: 별도 큐·필드·상태값을 만들지 않았다. 기존 복구 경로가 누락 작업 ID를 비잠금 일관 읽기로 먼저 메모리에 담고, 이후 문자 행만 `VALUES`로 멱등 삽입하도록 바꿨다. 동시에 같은 함수의 무효 후보 정리도 `작업 → 문자` 순서로 통일했다. 생산자와 복구기가 동시에 같은 멱등키를 만들면 고유키가 중복을 흡수하고, 조회 뒤 의무 상태가 바뀐 후보는 기존 최종 세대 검사가 발송하지 않는다.
- 검증 범위: 문자 Outbox 자체검사 4개를 기존 테스트 확장으로 통과했고, 이메일 수집 트랜잭션 24개, NOW-mode 자체검사, Node 문법 검사, Python 문법 검사와 `git diff --check`를 통과했다. 운영 MariaDB에서 새 누락 후보 SELECT가 정상 실행됐고 현재 누락 의무는 0건이며 이벤트 `#862`는 `done/finalized`, 문자는 `sent` 1회로 유지됐다. 신규/레거시 플랫폼 작업 로직과 실제 외부 클릭은 변경하지 않아 재실행하지 않았고, 운영 데이터 수정이나 배포도 하지 않았다.
- 관련 파일: `tools/spacecloud-watch.mjs`, `ops/rhythmjoy_sms_outbox_selftest.py`.

## 2026-08-25 — 동기화 이전 네이버 내보내기 취소 2건 정합화

- 상태: 운영 데이터와 실제 스페이스클라우드 일정의 동기화 완료. 건별 코드 예외는 추가하지 않았다.
- 증상과 경로: 관리자 패널의 중요 오류 4개는 네이버 내보내기로 만들어진 2026-09-20·2026-10-04 예약 2건이 각각 취소 이메일 이벤트와 삭제 작업 알림으로 중복 집계된 결과였다. 취소 판정은 맞았지만, 해당 예약 세대에는 현재 자동화가 요구하는 최초 확정 이메일·업로드 작업 연결이 없어 안전장치가 삭제 부작용을 격리했고 원장과 외부 일정이 그대로 남았다.
- 기존 보호 목적: 확정 세대 증거가 없는 취소를 자동 실행하지 않는 규칙은 다른 고객 일정 오삭제를 막기 위한 실패 폐쇄 장치다. 이 규칙과 신규 예약의 `taskId + 예약번호` 식별은 유지했다.
- 운영 처리: 각 건의 원장·확정/취소 이벤트·삭제 작업·예약번호·홀·날짜·시간을 대조하고, 로그인된 스페이스클라우드에서 정확히 일치하는 후보가 한 건씩이며 다른 후보가 없음을 확인했다. 두 일정만 직접 삭제한 뒤 API 반복 조회에서 후보 0건을 확인하고, 전체 DB 백업을 보존한 상태에서 기존 수동 복구 선례와 동일한 종료 상태로 원장·이벤트·작업을 한 트랜잭션에서 정합화했다. 10월 4일의 다른 시간대 예약은 보존했다.
- 결과: 공개 캐시를 재생성했고 반영 감사 551/551 정상, 수집 감사 149건·누락 0건, 공개 일정 불일치·중복 0건이었다. 미래 실행 대기 작업과 미해결 취소 이벤트, 실제 플랫폼 감사 문제는 모두 0건이며 관리자 패널의 중요·활성·미확인 알림도 0건이다.
- 변경 경계: 이번 두 건은 동기화 도입 이전 데이터의 운영 정합화로 끝냈다. 같은 유형을 자동 처리하는 건별 분기를 만들지 않았고, 스페이스클라우드 원천 예약의 취소 후 재예약 세대 판별은 아래 공통 원인 수정에서 기존 원장·이벤트 순서·`booking_ledger_id`를 재사용한다.

## 2026-08-20 — 스페이스클라우드 예약 세대와 취소 판별 책임 통합

- 상태: 근본 원인 수정과 로컬·읽기 전용 운영 검증 완료. 이 기록의 검증 단계에서는 기존 운영 데이터와 작업 상태를 변경하지 않았다.
- 기존 보호 목적: 7월 자연키는 예약번호가 없는 스페이스클라우드 취소를 `방·사용일·시간·정규화 예약자명`으로 현재 원장에 연결하기 위해 도입됐다. 7월의 이메일 이벤트 작업키는 같은 시간의 취소 후 재예약을 별도 작업으로 보존하기 위해 추가됐다. 두 안전장치의 목적은 유지한다.
- 근본 원인: 작업 큐는 이메일 이벤트 ID로 예약 세대를 구분하지만 원장은 같은 자연키를 한 행에 계속 덮어써, 취소 갱신과 감시기가 다시 최신/과거 세대를 추론했다. 취소 원장 갱신은 사전에 선택한 원장 ID를 갱신한 뒤 동일 속성의 다른 비네이버 원장까지 한 번 더 넓게 갱신해 판별 책임도 중복됐다.
- 통합 기준: 네이버는 기존과 같이 예약번호가 원장 식별자다. 스페이스클라우드는 `방·사용일·정규화 예약자명`을 필수 사업 식별값으로 유지하고, 신규 확정메일은 스페이스클라우드 내부 ID가 있으면 그 ID를, 없으면 기존 이메일 이벤트 ID를 같은 예약의 세대 증거로 사용한다. 사용시간은 취소 후보와 부작용 범위를 좁히는 값이며 영구 예약 식별값으로 취급하지 않는다. 수신 순서가 취소보다 엄격히 이전인 활성 후보가 정확히 하나일 때만 취소 세대로 연결한다.
- 실패 폐쇄: 같은 수신 순서의 확정/취소 충돌, 순서를 확인할 수 없는 후보, 이전 활성 후보가 둘 이상인 경우, 활성 원장 없이 과거 이메일만 있는 경우에는 어떤 활성 원장도 취소하지 않는다. 과거 이벤트는 진단 근거로만 남기며 네이버 복구 작업은 기존 `needs_review` 상태로 저장해 자동 실행하지 않는다.
- 중복 제거: 스페이스클라우드 취소 시 정확히 선택된 원장 세대만 갱신하고, 같은 방·날짜·시간·예약자명의 다른 비네이버 원장을 다시 검색해 취소하던 두 번째 광역 갱신을 제거했다. 네이버 차단/복구 작업은 새 필드나 큐를 만들지 않고 기존 `booking_ledger_id`에 원장 세대를 연결하며, 감시기도 동일 원장 ID의 차단 작업을 우선 사용하고 다른 세대 ID는 제외한다.
- 레거시 경계: 기존 자연키 함수와 과거 원장 763건은 호환 경로로 유지한다. 비파괴 백필은 이미 확정/취소 이벤트 ID가 원장에 투영된 스페이스클라우드 세대를 건너뛰어 신규 세대 원장 옆에 자연키 중복 행을 만들지 않는다. 과거 원장을 일괄 마이그레이션하거나 종료 예약을 재검증 대상으로 확대하지 않았다.
- 검증: 이메일 수집 자체검사 24개, 원장 이벤트 순서 검사, 동일 초 충돌 검사, Node 문법 검사, NOW-mode 자체검사, 예약 충돌 정책 검사와 SpaceCloud 업로더 검사 18개를 통과했다. 운영 DB 읽기 전용 검사에서 미래 스페이스클라우드 활성 16건 모두 확정 이메일·순서가 연결됐고 순서 누락 0건, 동일 정확 식별 활성 중복 그룹 0건이었다. 내부 ID가 없는 확정 이벤트 7건도 모두 현재 원장에 연결돼 있고 같은 방·사용일·시간·예약자명의 반복 그룹은 0건이었다. 저장된 확정 119건·취소 25건을 순서대로 재생한 결과 취소 22건은 유일한 이전 활성 세대, 선행 확정이 없는 레거시 3건은 자동 실행 금지로 분류됐으며 충돌·모호 판정은 0건이었다. 새 취소 후보 SQL, 원장 연결 작업 SQL, 백필 투영 확인 SQL은 현재 MariaDB 스키마에서 파싱됐다.
- 관련 파일: `ops/rhythmjoy_email_import.py`, `ops/rhythmjoy_email_import_selftest.py`, `tools/spacecloud-watch.mjs`, `docs/spacecloud-sync.md`.

## 2026-08-20 — 구형 스페이스클라우드 취소 4건의 허위 부재 판정

- 상태: 근본 원인 수정, 누락 전건 실제 삭제, 독립 플랫폼 재검증 및 운영 반영 완료. 커밋 `0d9a1a1`과 `60bdb87`을 기능 브랜치와 `main`에 푸시한 뒤 Ubuntu 감시기에 배포했다.
- 현상: 네이버 취소메일로 생성된 2026-08-28 A홀, 8월 31일·9월 1일·9월 4일 B홀 20:00-22:00 삭제 작업 `#728`-`#731`이 DB에서는 `already_gone`이었지만 스페이스클라우드에는 예약번호 `1289392069`, `1289397720`, `1289400914`, `1289402981` 일정이 각각 남아 있었다. 네 건 모두 실제 일정 후보 1건과 예약번호·홀·날짜·시간·예약자명이 일치했으며 삭제 클릭은 실행되지 않았다.
- 회귀 원인: 8월 11일 커밋 `504a4ee`에서 신규 업로드의 중복·오인 검증을 막기 위해 `예약번호 + 내부 taskId` 일치를 도입했다. 8월 16일 커밋 `3881d19`의 취소 Saga 강화가 이 검증기를 삭제에 재사용하면서, 7월의 `manual-live-platform-verification` 구형 일정에도 존재하지 않는 `taskId` 메모를 필수로 요구했다. 같은 변경의 부재 폴링은 `실제 슬롯 후보 0건`이 아니라 `엄격 식별 후보 0건`을 부재로 계산해, 예약번호가 정확한 후보를 `task-id-mismatch`로 제외한 뒤 연속 부재로 오판했다. 최근 취소메일 격리 복구가 이 잠복 회귀를 처음 실행시켜 드러냈다.
- 근본 개선: 신규 일정의 엄격한 `taskId + 예약번호` 보호는 유지한다. DB에서 정확한 원장·확정 이벤트·구형 업로드 세대와 `manual-live-platform-verification` 출처가 증명된 경우에만, 메모에 다른 `taskId`가 없고 예약번호·홀·날짜·시간·예약자명이 모두 맞는 일정에 구형 식별 모드를 허용한다. API와 삭제 팝업을 같은 조건으로 검증하며 다른 예약번호·이름·비어 있지 않은 다른 `taskId`는 계속 차단한다.
- 부재·재예약 보호: 같은 예약번호 또는 식별정보가 불완전한 슬롯 후보는 절대 부재로 계산하지 않는다. 다른 예약번호가 명시되고 대상 `taskId`를 갖지 않은 같은 시간 후속 예약만 취소 대상과 다른 일정으로 증명해 보존한다. 최종 DB 삭제 체크포인트도 모호 후보 수 0과 다른 예약 후보 수/전체 후보 수 일치를 다시 확인한다. 과거 허위 정리로 `skipped`가 된 구형 실제 일정은 빠른 `already_gone` 경로를 타지 않고 반드시 플랫폼을 다시 읽는다.
- 인접 개선: 고객 실제플랫폼 감사가 취소 이메일 시각만 조회해 관리자 화면의 수동 고객 취소를 제외하던 누락을 함께 수정했다. `automation_canceled_at → canceled_email_received_at → last_event_at` 순서의 최근 취소 기준을 조회와 감사 해제에 동일하게 적용한다. `/Users/inteyeo/.codex/AGENTS.md`에는 모든 오류 수정 전 원래 안전장치와 Git 이력을 조사하고, 원래 불변조건·신규/구형·자동/수동·성공/실패/재시도 경로의 재현·회귀 테스트를 함께 수행한다는 전역 규칙을 추가했다.
- 운영 복구: 재처리 전 전체 DB 백업 `/home/clown313python/rhythmjoy_ops/backups/db/raj-20260820-133503.sql.gz`을 생성했다. 원장, 확정·취소 이벤트, 구형 업로드, 허위 부재 결과, 삭제 미시도, 실제 플랫폼 감사와 일정 ID가 모두 일치할 때만 한 트랜잭션으로 네 구형 업로드를 재검증 대기, 삭제 작업을 `pending/ready`로 되돌렸다. 감시기가 일정 `9448358`, `9448382`, `9448532`, `9448534`를 각각 다시 식별해 실제 삭제했고, 네 작업 모두 `deletionAttempted=true`, `deleted/finalized`, 삭제 후 API 후보 0건으로 완료됐다.
- 전수 검증: 독립 고객 실제플랫폼 감사에서 위 네 원장과 비교 대상 2026-10-24 자동 취소·2026-08-23 수동 취소까지 총 6건 모두 `ok`, 실제 후보 0건이다. 8월 19일 이후 취소 이벤트 `#749`, `#756`, `#761`-`#764`는 모두 파싱됐고 정확히 하나의 종료 작업 세대와 연결된다. 미래 `pending/running/failed/needs_review` 작업 0건, 고객 플랫폼 감사 문제 0건, 최근 수집 문제 0건이다. 반영 감사는 486/486 정상, 수집 감사 163건·누락 0건, 공개 일정 불일치·중복·대기 모두 0건이다.
- 테스트·배포: 업로더 테스트 17개, 취소 Saga 경계 테스트 12개, NOW-mode 자체검사, Node 문법·diff 검사, 이메일 수집 자체검사 15개, 이벤트 순서·원장 불변조건·SMS·반영 감사 자체검사를 통과했다. 운영 감시기와 업로더 해시는 각각 `64ce2e850ea01b77c8dd14f5a9113b362c7deca31551521b337de506e46ec01a`, `32cbc8c3ccb1a05d87561277d8c1ff113c4344520dbeb8e62ab14c6cca82dc25`로 배포본과 일치하며 감시기·메일 수집기·공개 캐시·감사 타이머는 모두 `active`다.
- 관련 파일: `tools/spacecloud-playwright-uploader.mjs`, `tools/spacecloud-playwright-uploader-test.mjs`, `tools/spacecloud-watch.mjs`, `ops/rhythmjoy_reflection_audit.py`, `ops/rhythmjoy_reflection_audit_selftest.py`.

## 2026-08-20 — 수정 전 기존 구현 재사용 검증을 필수 관문으로 명문화

- 상태: 전역 Codex 작업 규칙 보강 완료. 이 규칙 정리 자체로 애플리케이션 구조를 추가하지 않았다.
- 배경: 스페이스클라우드 취소 후 재예약 구분을 검토하면서, 기존의 수신 시각·`event_order_key`·이메일 이벤트 ID 기반 세대 판정을 충분히 확인하기 전에 새 원장 식별 구조를 제안했다. 그대로 구현했다면 같은 책임을 두 구조가 나눠 갖고 원인과 결과가 더 혼동될 수 있었다.
- 실용 관문: 코드·DB·작업 큐·운영 절차를 바꾸기 전에 관련 심볼과 호출부, 스키마·마이그레이션, 작업 종류·상태값, 설정, 테스트, 문서, `git blame/log`를 조사해 `기존 구현 있음 / 일부 있음 / 없음`을 근거와 함께 판정한다. `있음`은 코드 추가 없이 기존 테스트·설정·운영 복구로 해결하고, `일부 있음`은 정확한 연결 누락만 보완하며, `없음`일 때만 새 구조를 검토한다.
- 강화의 한계: 검사 항목을 무조건 늘리는 방식도 또 다른 땜질이 될 수 있으므로, 오류를 만든 기존 안전장치의 목적과 현재 불변조건을 먼저 적고 그 목적을 보존하는 최소 변경을 선택한다. 새 함수·필드·테이블·큐·상태값은 기존 구조로 요구사항을 충족할 수 없는 이유와 마이그레이션·충돌 영향을 입증할 때만 추가한다.
- 이번 판정: 스페이스클라우드 원천 예약의 안정 식별은 기존의 예약 사용일·예약자명·방 복합 기준을 재사용하고, 셋 중 하나라도 다르면 다른 예약으로 본다. 메일 수신 시각은 원장 식별값을 중복 확장하는 용도가 아니라 같은 예약의 취소 후 재예약 세대를 초 단위 이상으로 정렬하는 기존 이벤트 순서 체계에서 사용한다.
- 완료 기준: 수정 전에는 재사용 대상·정확한 빈틈·원래 안전장치의 목적을 기록하고, 완료 후에는 중복 구현 미발생과 신규/구형·자동/수동·성공/실패/재시도·인접 플랫폼 경로의 회귀검사 결과를 남긴다.
- 관련 파일: 전역 `~/.codex/AGENTS.md`, `ops/rhythmjoy_email_import.py`, `tools/spacecloud-watch.mjs`.

## 2026-08-18 — 스페이스클라우드 예약확정 문자 조기 안전검사 오판

- 상태: 해결 및 운영 반영 완료. 커밋 `fdb4bc0`을 `main`에 푸시한 뒤 Ubuntu 미니PC 감시기에 배포했다.
- 현상: 스페이스클라우드 예약의 네이버 예약불가 반영은 검증 완료됐지만 Telegram 완료 알림에 `문자 발송 생략`이 표시됐다. 2026-08-16 22:40 이후 같은 경로 5건이 `skipped`, Aligo 발송 시도 0회로 남았다. 고객 전화번호 조회와 네이버 반영은 정상이었다.
- 원인: 취소와 확정문자의 경합을 막는 최종 세대 검사는 플랫폼 작업이 DB에서 `done`이고 결과가 `blocked` 또는 `already-blocked`일 때만 발송을 허용한다. 네이버 예약불가 실행기는 이 완료 상태를 저장하기 전에 문자를 직접 호출해, 실제 성공 작업을 `아직 플랫폼 반영 확정 전`으로 오판했다. 네이버 원천 예약의 스페이스클라우드 등록 경로는 이미 완료 저장 후 문자 Outbox를 실행했지만 스페이스클라우드 원천 경로에만 과거 직접 호출이 남아 있었다.
- 조치: 네이버 예약불가 성공 시 직접 문자를 호출하지 않고 발송 의무를 유지한 채 작업 결과를 먼저 커밋한다. 같은 주기의 독립 SMS Outbox가 커밋된 원장 세대와 플랫폼 결과를 다시 잠금 검증한 뒤 발송한다. 작업이 `pending` 또는 `running`이면 영구 생략하지 않고 완료 후 재시도하도록 이중 방어를 추가했으며, 기존 Outbox 행을 발송할 때 전화번호 해시와 끝 네 자리도 함께 기록한다.
- 운영 복구: 생략 5건을 원장·작업·발송 시도 이력으로 다시 잠금 검증했다. 이미 이용시간이 지난 2건은 재발송하지 않았고, 아직 유효한 2026-08-19 예약 3건만 기존 멱등키로 재대기했다. 세 건 모두 Aligo 응답 코드 `1`, 시도 1회, `sent`로 완료됐고 문자 의무 표시는 해제됐다.
- 검증: Node 문법 검사, NOW-mode 자체검사, SMS Outbox 자체검사 4개, Saga 경계 테스트 12개, 이메일 수집 자체검사 15개, 네이버 시간칸 테스트 4개를 통과했다. 운영 감시기 해시는 배포본 `e61797281dbd63a388ad8f6df5ca76d96ca703ccae0c703986f4512015ea3f55`와 일치하고 서비스는 `active`다. 필수 Outbox 누락, 미완료 확정문자, 미래 확정 예약의 문자 생략, 최근 문자 문제 상태는 모두 0건이다.
- 관련 파일: `tools/spacecloud-watch.mjs`.

## 2026-08-14 — 관리자 일정 취소 진행상태 무표시

- 상태: 해결 및 운영 반영 완료. 기능 커밋 `cbc47d0`과 운영 보안규칙 호환 캐시 키 커밋 `d6826f2`를 `main`에 푸시한 뒤 Cafe24에 배포했다.
- 현상: 관리자 추가 일정에서 취소를 확인하면 `취소·복구 작업을 만들었습니다.`라는 일회성 토스트만 표시되고 일정 블록의 모양은 그대로였다. 등록 작업에만 연결된 지속형 상태 카드가 취소 작업에는 연결되지 않아, 스페이스클라우드 삭제와 네이버 예약 가능 복구가 실제로 진행되는지 같은 화면에서 확인할 수 없었다.
- 운영 확인: 최근 취소 건인 2026-08-20 A홀 15:00-17:00 관리자 일정 `#8`은 DB `canceled`, 스페이스클라우드 삭제 `done`, 네이버 예약 가능 복구 `done`으로 정상 완료됐다. 기능 실패가 아니라 완료 결과를 설명하지 못한 UI 문제였다.
- 조치: 단건 취소 응답에 정확한 예약 ID와 취소 전용 작업 상태를 반환하고, 등록 상태 카드를 취소에도 재사용해 `접수 → 처리 중 → 완료/확인 필요`를 3초마다 갱신한다. 취소 중인 일정 블록은 주황색 점선과 `취소 처리 중 · SC 삭제 · 네이버 복구 확인 중` 문구로 구분하며, 두 작업 완료 뒤 일정표에서 자동 제거한다. 등록 완료 카드를 닫았던 예약도 이후 취소 카드는 별도 작업으로 다시 표시한다.
- 검증: 운영 PHP 문법 검사, MariaDB 연결 범위 임시 테이블 자체검사, Node 문법 검사와 diff 검사를 통과했다. 1440×1100 및 390×844 모의 DB 브라우저에서 취소 접수 상태, 두 플랫폼 단계, 완료 후 일정 0건, 새로고침 복원, 가로 오버플로 없음, 콘솔 오류 0건을 확인했다. 첫 배포의 `admin-cancel-…` 캐시 식별자는 Apache 보안규칙에서 정적 자산 403을 일으켜, 사전 HTTP 확인을 통과한 `cancel-progress-…`로 즉시 교체했다. 최종 운영 브라우저는 HTTP 200, `DB 큐`, `일정 취소 완료`, 네이버 복구 완료, 스페이스클라우드 삭제 완료, 가로 오버플로 없음, 콘솔 오류 0건이며 로컬·release·운영 웹 파일 해시가 일치한다.
- 관련 파일: `www/sync-admin/api.php`, `www/sync-admin/app.js`, `www/sync-admin/styles.css`, `www/sync-admin/index.html`, `tools/sync-admin-ui-mock.mjs`, `ops/rhythmjoy_sync_admin_idempotency_selftest.php`.

## 2026-08-14 — 고객 요청 취소 기능·겹침 해제 후 등록 재시도 오판

- 상태: 해결 및 운영 반영 완료. 기능·재발 방지 커밋 `0927964`를 `main`에 푸시한 뒤 Cafe24와 Ubuntu 미니PC에 배포했다.
- 고객 요청 취소: 주간 일정에서 네이버·스페이스클라우드 고객 예약 하나를 누르면 상세 화면에 `고객 요청 취소` 버튼이 표시된다. 확인창은 네이버와 스페이스클라우드 반영 및 원천 예약 환불 절차를 안내하고 `진행 안 하기`·`확인`을 제공한다. 관리자 추가 일정은 기존 상세 취소를 유지하되 `관리자가 예약한 건이라 바로 취소됩니다.`라는 별도 확인창을 사용한다.
- 안전 경계: 고객 취소 요청은 DB 원장을 바로 삭제하지 않고 원천 플랫폼 취소 작업을 멱등 키로 한 번만 만든다. 감시기는 실행 직전과 최종 확인 직전에 작업 claim, 원장 ID·키, 원천 플랫폼, 확정 메일 ID, 홀·날짜·시작·종료, 네이버 예약번호 또는 스페이스클라우드 예약 ID를 모두 다시 대조한다. 플랫폼 취소가 식별값으로 확인된 뒤에만 원장을 `canceled`로 바꾸고 반대 플랫폼의 복제 일정 삭제 또는 예약 가능 시간 복구 작업을 같은 트랜잭션으로 만든다. 제출 체크포인트 뒤에는 취소 버튼을 다시 누르지 않고 실제 상태만 재검증한다.
- 작업 `#633`: 기존 2026-08-14 D홀 17:00-20:00 예약 원장 `#9258`을 고객 요청으로 수동 취소한 뒤, 새 17:00-19:00 예약 원장 `#9259`의 스페이스클라우드 등록이 겹침 판정에서 풀렸지만 재대기 결과에 `submissionAttempted=false` 근거가 없어 검증 전용 상태로 잘못 전환됐다. 겹침 해제 재대기 결과에 명시적인 제출 전 안전 재시도 표식을 저장하도록 이메일 수집기와 감시기를 함께 보완했다. `#633`은 최종 `done`, 제출 후 정확한 예약 식별 검증 완료이며 `#9259`는 `confirmed`다.
- 허위 감사 `ingestion:email:668`: 최신 확정 메일 뒤 운영자가 고객 요청으로 원장을 취소한 경우를 메일 상태 불일치로 다시 경고하지 않도록 취소 감사 payload를 판정에 포함했다. 운영 재검사에서 `ingestion:email:668`과 `ledger:9259:upload`는 모두 `ok`, 전체 반영 감사 871건은 전부 `ok`, 관리자 알림은 활성·중요·미확인 모두 0건이다.
- 검증: PHP 5.4 운영 자체검사, Python 수집·반영 감사 자체검사, Node 문법 검사와 NOW-mode 자체검사를 통과했다. 모바일 390×844 모의 화면에서 고객·관리자 경고창 문구와 버튼, 오버플로 없음, 비드래그 속성, 콘솔 오류 0건을 확인했다. 공개 DB 일정에는 취소 원장 `#9258`이 없고 새 원장 `#9259`만 17:00-19:00으로 표시된다. 배포 파일 해시는 로컬·Cafe24·release·미니PC가 각각 일치하고 감시 서비스는 10초 NOW-mode를 유지한 채 `active`·idle이다.
- 관련 파일: `www/sync-admin/index.html`, `www/sync-admin/app.js`, `www/sync-admin/styles.css`, `www/sync-admin/api.php`, `tools/spacecloud-watch.mjs`, `tools/spacecloud-playwright-uploader.mjs`, `ops/rhythmjoy_email_import.py`, `ops/rhythmjoy_reflection_audit.py`.

## 2026-08-14 — 당일 관리자 예약의 원장 키 오판·지난 네이버 시간칸 중단

- 상태: 해결, 운영 복구 및 배포 완료. 원장 키 전달 보완 커밋 `4a5ee53`과 지난 시간칸 처리 커밋 `32c0648`을 `main`에 푸시했다.
- 현상: 관리자 예약 `#9`(2026-08-14 A홀 12:00-16:00, `B홀a로이전`)은 스페이스클라우드 작업 `#629`가 등록 완료됐지만 네이버 예약불가 작업 `#628`이 처음에는 `current-ledger-not-found`, 재시도에서는 이미 시작된 12:00-13:00 칸의 `잔여예약 1` 상태를 읽고 `needs_review`로 멈췄다. 관리자 예약은 `pending`으로 남고 확인 필요 알림이 표시됐다.
- 원인 1: PHP 관리자 API는 예약자명의 라틴 대문자를 소문자로 정규화해 원장 키를 만들지만 Python 감시기는 이메일 예약의 대소문자를 보존한다. 이름에 대문자 `B`가 포함된 관리자 작업이 원장 키를 다시 계산하면서 실제 PHP 키와 달라졌고, 존재하는 관리자 원장을 누락으로 오판했다.
- 원인 2: 네이버는 당일 시간칸의 시작 시각이 지나면 그 칸을 예약 가능/불가 편집 버튼 대신 비활성 정보로 표시한다. 자동화가 요청 범위의 첫 칸부터 전부 편집하려 해 이미 시작된 칸에서 중단됐고, 뒤의 아직 활성화된 칸까지 처리하지 못했다.
- 조치: 관리자 API가 트랜잭션에서 만든 정확한 `ledger_key`와 원천 플랫폼을 각 실작업 payload에 함께 저장하고, 과거 관리자 작업에만 PHP 호환 소문자 키를 한 번 재조회한다. 네이버 차단·복구는 한국 시각 기준으로 시작 시각이 지난 시간칸을 명시적으로 `skipped-started-slot`로 기록해 생략하고 아직 시작하지 않은 시간칸만 변경·재검증한다. 전 구간이 이미 시작된 경우도 실패가 아니라 `elapsed-no-action`으로 종료한다. 완료 안내에는 생략한 지난 칸 수를 포함한다.
- 운영 복구: 두 실패 모두 `appliedSlots`가 비어 있어 플랫폼 변경이 없음을 행 잠금과 작업 식별값으로 확인한 뒤에만 `#628`을 재대기 처리했다. 최종 실행 시각에는 12:00-13:00과 13:00-14:00가 이미 시작돼 두 칸을 생략했고, 14:00-15:00과 15:00-16:00를 각각 `available → suspended`로 변경한 뒤 재조회 검증했다. `#628`과 `#629`, 연결된 관리자 작업 `#25`와 `#26`은 모두 `done`, 관리자 예약 `#9`은 `confirmed`, 두 관리자 원장 앵커는 `confirmed`다.
- 검증: Node 문법 검사, 네이버 단위 테스트 4개, 감시기 자체검사, PHP 5.4 문법 검사와 운영 MariaDB 임시 테이블 멱등성 검사를 통과했다. 두 플랫폼 세션은 `ready`였고 감시기는 재처리 후 `naver-availability-processed` 및 후속 idle 주기를 기록했다. 작업 `#628` 결과는 처리 대상 2칸·변경 2칸·검증 성공 2칸이며, `#629`는 `submitted`와 제출 후 확인을 보존한다. 13:07 독립 실제 플랫폼 재검사도 관리자 예약 `#9`의 스페이스클라우드 예약과 네이버의 아직 유효한 차단 시간칸을 각각 `ok`로 확인했고 불일치·재검사 대기·검사 실패는 모두 0건이었다. 해당 작업의 중요 알림 조건은 해소됐다.
- 배포: Cafe24 API 해시는 로컬·운영·release가 `6720c9e485b9d584163f4544c4003f5f988b4cd645ee014d3c2a14c73399d294`로 일치한다. 최종 Ubuntu 미니PC의 네이버 모듈과 감시기 해시는 각각 `9a709709064b80897149ebe65f4023052d64db135c85ae054965d0949adb8f28`, `bf5cbca6e4f51955358c7fb099b78dec15e9431e5f9987b71272d0ff147df3ef`이며 서비스는 `active`다. 배포 전 파일은 Cafe24 `backups/deploy-4a5ee53`과 미니PC `deploy-backups/4a5ee53`, `deploy-backups/32c0648`, `deploy-backups/d4f52f6`에 보존했다.
- 관련 파일: `www/sync-admin/api.php`, `tools/spacecloud-watch.mjs`, `tools/naver-playwright-availability.mjs`, `tools/naver-playwright-availability-test.mjs`, `ops/rhythmjoy_sync_admin_idempotency_selftest.php`.

## 2026-08-13 — 관리자 예약 진행상태 누락·스페이스클라우드 사전판정 중단

- 상태: 해결 및 운영 반영 완료. 코드 커밋 `5871ffa`와 완료 카드 복원 보완 커밋 `a4d8195`를 `main`에 푸시해 Cafe24 웹과 Ubuntu 자동화 PC에 배포했다.
- 현상: 관리자 단건 예약을 등록하면 `DB에 동기화 작업을 생성했습니다.`라는 일회성 문구만 나오고, 접수·처리 중·완료·확인 필요 여부를 같은 화면에서 계속 확인할 수 없었다. 운영 예약 `#8`(2026-08-20 A홀 15:00-17:00)은 네이버 예약불가 작업 `#619`는 완료됐지만 스페이스클라우드 등록 작업 `#620`이 `current-ledger-not-found`로 중단됐다.
- 원인: 관리자 입력은 이메일 확정 이벤트가 없는 별도 DB 원장 앵커를 사용한다. 그런데 스페이스클라우드 등록 직전 충돌 검사에서 관리자 작업에도 `가장 먼저 도착한 확정 이메일 예약 우선` 규칙을 적용했고, 검사 쿼리는 관리자 앵커를 의도적으로 제외하므로 현재 이메일 원장을 찾을 수 없었다. 실제 플랫폼 충돌이나 스페이스클라우드 제출 실패가 아니라 제출 전 잘못된 정책 분기였다.
- 조치: 관리자 스페이스클라우드 등록은 실제 플랫폼 원장 겹침이 없으면 업로더로 계속 진행하고, 등록 대기 중 실제 예약이 새로 생긴 경우에만 기존 예약을 자동 취소하지 않고 확인 필요로 멈춘다. 관리자 화면에는 예약별 지속형 상태 카드를 추가해 네이버 예약불가와 스페이스클라우드 예약등록을 각각 `대기 → 처리 중 → 완료/확인 필요`로 표시한다. 3초 간격의 경량 상태 API를 사용하며 새로고침 뒤에도 최근 미완료·확인 필요 작업과 24시간 내 완료 작업을 복원한다. `current-ledger-not-found` 내부 키는 관리자에게 원인을 설명하는 한국어 문구로 바꿨다.
- 운영 복구: 작업 `#620`의 과거 결과에서 실제 제출 시도가 없고 스페이스클라우드 동일 시간 후보가 0건임을 확인한 뒤, 작업 번호·종류·예약번호·오류·제출 여부를 모두 조건으로 안전 재대기 처리했다. 제출 전 체크포인트를 보존한 최종 실행에서 스페이스클라우드 등록과 제출 후 동일 예약 검증이 완료됐다. 관리자 예약 `#8`은 `confirmed`, 네이버 작업 `#619`와 스페이스클라우드 작업 `#620`은 모두 `done`이다.
- 검증: Node 문법 검사와 감시기 자체검사를 통과했다. 로컬 DB 모의 브라우저에서 다른 날짜 단건 예약의 `접수 → 처리 중 → 양쪽 완료`, `네이버 완료 + 스페이스클라우드 확인 필요`, 새로고침 후 카드 복원을 확인했다. Cafe24의 PHP 5.4.16에서도 API 문법 검사와 상태 집계 자체검사를 통과했다. 운영 `reservation_status` API는 예약 상태 `confirmed`, 전체 상태 `done`, 두 작업 `done`을 반환하며 관리자 알림은 활성·중요·미확인 모두 0건이다. 웹 4개 파일과 Ubuntu 감시기의 SHA-256은 배포한 로컬 파일과 일치하고 감시 서비스는 `active`다.
- 관련 파일: `tools/spacecloud-watch.mjs`, `tools/sync-admin-ui-mock.mjs`, `www/sync-admin/api.php`, `www/sync-admin/app.js`, `www/sync-admin/index.html`, `www/sync-admin/styles.css`.

## 2026-08-13 — 30분 정규검사의 허위 중단 경고

- 상태: 해결 및 운영 반영 완료. 커밋 `afcd773`을 `main`에 푸시한 뒤 Cafe24에 배포했다.
- 현상: 17:31 관리자 화면에 `중요 오류 1건`과 `예약 정규검사가 멈췄습니다`가 표시됐지만, 실제 정규검사 서비스는 활성 상태였다.
- 원인: Cafe24 정규검사 타이머는 30분 간격인데 관리자 알림 API가 마지막 검사 후 12분부터 중단으로 판정했다. 따라서 정상 실행 사이의 약 18분 동안 매 주기마다 허위 중요 오류가 노출됐다. 당시 마지막 검사는 17:11:31에 413/413건 정상·문제 0건으로 완료됐고 다음 실행은 17:41:25로 정상 예약돼 있었다.
- 조치: 중단 판정을 40분으로 늘려 30분 실행 주기와 systemd 지연·검사 실행 시간을 허용한다. 중단 알림 서명에는 마지막 정상 검사 시각을 포함해, 복구 후 실제로 다시 중단되면 과거 읽음 처리에 묻히지 않고 새 알림으로 표시되게 했다.
- 검증: 운영 PHP 5.4 자체 테스트에서 31분 경과는 정상, 40분 경과는 중단으로 판정됐다. 배포 직후 운영 알림 API는 활성·중요·미확인 모두 0건이었다. 17:41 정규검사는 413/413건 정상·문제 0건으로 완료됐고 다음 18:11 실행이 예약돼 있으며, 타이머와 Apache·메일·DB 캐시 서비스는 모두 `active`다. 운영 `api.php` 해시는 배포 커밋과 일치한다.
- 관련 파일: `www/sync-admin/api.php`.

## 2026-08-13 — 관리자 예약 중복 경고 가림·날짜 선택 누락

- 상태: 해결 및 운영 반영 완료. 커밋 `afcd773`을 `main`에 푸시한 뒤 Cafe24에 배포했다.
- 현상: 모바일 예약 입력 모달에서 `동기화 작업 만들기`를 눌러도 화면 변화나 안내가 보이지 않았다. 같은 시각 Apache 접근 로그에는 `bootstrap`·`alerts`만 있고 `create_reservation` 요청은 없었다. 또한 모달 안에는 날짜 입력이 없어 현재 일정표 날짜가 아닌 예약을 바로 만들 수 없었다.
- 원인: `2026-08-13 A홀 15:00-17:00`에는 이미 활성 DB 원장 예약이 있어 브라우저의 제출 전 중복 검사가 정상 차단했다. 해당 원장의 스페이스클라우드 등록 작업은 `done`, 17:11 정규검사는 `ok`였다. 그러나 경고 토스트의 `z-index`가 `auto`이고 모달 배경은 `40`이라 안내가 모달 뒤에 가려져 무반응처럼 보였다.
- 조치: 단건 예약 오류를 모달 안의 지속형 경고 영역에 표시하고, 입력을 바꾸기 전까지 유지한다. 전역 토스트도 모달보다 높은 계층에 표시하며 모바일 안전영역과 최대 너비를 적용했다. 중복이면 기존 예약 출처·시간과 새 작업을 만들지 않았다는 결과를 명시한다. 모달 첫 항목에 날짜 선택기를 추가하고 선택 날짜를 요약·중복 검사·요청 식별값·API 저장에 사용한다. 등록 성공 후 일정표도 생성한 날짜로 이동해 결과를 즉시 보여준다.
- 검증: 운영 DB와 접근 로그는 읽기 전용으로 확인했으며 운영 예약·작업은 변경하지 않았다. 모바일 크기 로컬 DB 모의 화면에서 기존 날짜 중복 시 인라인 경고 노출·생성 API 미호출을 확인했고, 날짜를 다음 날로 바꾸면 선택 날짜가 요청과 일정표에 반영되며 생성 성공 후 해당 날짜로 이동함을 확인했다. 운영 화면에서 새 날짜 입력란과 정상 관리자 알림 상태를 확인했으며 `index.html`·`app.js`·`styles.css`·`api.php`의 운영 해시가 배포 커밋과 모두 일치했다.
- 관련 파일: `www/sync-admin/index.html`, `www/sync-admin/app.js`, `www/sync-admin/styles.css`, `tools/sync-admin-ui-mock.mjs`.

## 2026-08-12 — 관리자 중복 방지·Telegram 상태 알림·Chrome 충돌·Cafe24 용량 점검

- 상태: 해결 및 운영 반영 완료. 커밋 `e0cfd71`을 main에 푸시한 뒤 Cafe24와 미니PC에 같은 파일 해시로 배포하고 운영 재검증했다.
- 관리자 단건 등록: 단건 등록도 브라우저가 생성한 요청 ID를 반드시 서버에 전달한다. 서버는 요청 ID의 SHA-256 키로 예약을 식별하며, 같은 내용의 응답 유실 재시도는 기존 예약을 반환하고 같은 ID에 날짜·홀·시간·예약자·연락처·메모가 달라지면 거부한다. 버튼 연타는 첫 요청 중 비활성화한다.
- 정기대관 재시도: 기존에는 첫 등록 뒤 같은 요청을 다시 보내면 방금 생성한 자기 일정과 충돌해 중복 확인 전에 실패할 수 있었다. 이제 저장된 요청 서명을 충돌 검사보다 먼저 확인한다. 화면 표시용 충돌 필드는 서명에서 제외하지만 실제 규칙·날짜별 예외가 달라지면 같은 요청 ID를 재사용할 수 없다.
- DB 원장 실패 경계: API 인증·네트워크·서버·충돌 오류가 나면 더 이상 브라우저 로컬 초안을 일정으로 만들지 않는다. 같은 입력의 불확실한 재시도에는 같은 요청 ID를 보존하며, DB 응답으로 등록이 확인된 뒤에만 화면 일정에 나타난다. 실제 브라우저 모의검사에서 더블클릭은 서버 호출 1회, 503 뒤 재시도는 동일 요청 ID 2회, 충돌 응답 뒤 로컬 일정 0건이었다.
- 실제 MariaDB 검증: 연결 범위의 임시 테이블로 단건 최초/동일 재시도/변경 재사용 거부, 정기대관 최초/동일 재시도/변경 재사용 거부, 예약당 관리자 작업 정확히 2건을 확인했다. 운영 예약·작업·플랫폼에는 시험 행을 만들지 않았다.
- Telegram 상태 저장: 알림 상태 파일을 저장소 작업 디렉터리가 아니라 `/home/kiosk-j/rhythmjoy-logs/spacecloud-watch/notify-state.json`에 영구 보관한다. 최초 설치 시 기존 파일을 한 번 복사하고 이후 JSON은 임시 파일 작성 후 원자적 이름 변경으로 저장해 재부팅·배포·중간 종료 때 중복 억제 기록이 깨지지 않게 한다.
- Telegram 중복 기준: 같은 예약의 `submitted`, `already-blocked`, `already-available` 같은 구현 경로 차이는 모두 `검증 완료` 한 상태로 합친다. 확인 필요는 예약·작업 종류·상태·원인 지문이 같을 때만 억제하며, 관리자 정기대관 오류는 묶음 전체가 아니라 실제 문제가 난 예약별로 구분한다. 관리자 다건 정상 완료는 예약 수만큼 Telegram을 보내지 않고 관리자 DB 화면에서 확인하며, 오류만 해당 예약 상태가 바뀔 때 한 건 보낸다.
- 22:41 세션 경고 정정: 네이버와 스페이스클라우드가 동시에 만료된 것이 아니다. 두 검사 모두 `page.waitForTimeout: Page crashed`였고 같은 시각 Chrome 충돌 덤프가 생성됐다. 미니PC는 가용 RAM 4.9GB, 디스크 83% 여유, OOM 0건이었다. 새 컨텍스트의 독립 검사에서 두 플랫폼 모두 `ok=true`, 강제 주기검사에서 `ready · 인증 쿠키 유지`가 확인돼 22:47 복구 상태가 발송됐다.
- Chrome 충돌 자동복구: `Page crashed`, 브라우저 연결 종료, 브라우저 프로세스 충돌을 닫힌 컨텍스트와 같은 기반 장애로 분류한다. 세션 검사뿐 아니라 관리자·고객 실제 플랫폼 대조도 문제 알림과 DB 감사 저장을 보류한 채 컨텍스트를 한 번 재생성하고 즉시 다시 검사한다. 복구되면 아무 문제 알림도 보내지 않으며, 재생성 뒤에도 실패한 경우에만 `로그인 만료 확정 아님 · 화면 검사 장애`로 알린다. 실제 `login_required`만 수동 로그인 안내를 보낸다.
- 당일 Telegram 감사: 수정 전·복구 포함 상태변경 발송은 14건, 고유 상태도 14건, 전송 실패 0건이었다. 동일 상태 억제 59회는 사용자에게 재발송되지 않았다. 이 중 22:41의 세션 문제 2건과 22:47 복구 2건은 위 Chrome 충돌을 구버전이 로그인 장애로 오분류한 기록이다.
- Cafe24 VPS 사양: 4 vCPU, RAM 990MB, Swap 4GB, 할당 디스크 30GB(루트 사용 가능 25GB)다. 현재 루트 13GB 사용/11GB 여유/55%, inode 15%, MariaDB 물리 디렉터리 382MB, 앱 DB 논리 데이터 26.641MB·24개 테이블·정확 집계 약 1.2만 행이다. 별도 웹호스팅 DB MB 쿼터가 아니라 `/var/lib/mysql`이 루트 파일시스템을 쓰는 VPS 구조다.
- 용량 증가: DB 백업은 현재 약 2MB/일이며 30일 보존으로 약 55MB를 사용한다. `/var/log`는 1.2GB이고 주간 4회 회전, 앱 런타임 로그는 25MB다. 현재 증가 속도에서 DB가 즉시 용량 한도에 닿을 위험은 없다. MariaDB 5.5, 1MB `max_allowed_packet`, 128MB InnoDB 버퍼, 최대 연결 151/실제 최고 24이며 현재 48KiB 작업결과는 이 한도 안이다.
- 발견한 필드 한도: 과거 Google 달력 감사 5건의 `detail_json`이 `TEXT` 최대 65,535바이트에서 잘려 유효하지 않은 JSON으로 남아 있었다. 이미 잘린 과거 전문은 복구할 수 없지만 모두 과거 `calendar:*` 정상 기록이며 현재 DB 원장 감사에는 쓰지 않는다. 정규검사와 관리자 실제 플랫폼 감사의 상세근거를 `MEDIUMTEXT`로 바꾸고 기존 `TEXT` 스키마도 자동 승격한다. MariaDB 임시 테이블에서 자료형 마이그레이션을 검증했다.
- 최근 10일 재대조: 최신 유효 이메일 이벤트 180건은 예약 129, 취소 16, 스페이스클라우드 예약 31, 취소 4이며 모두 상대 플랫폼 작업 `done`, 이메일→DB 원장/작업 누락 0건이다. 최근 범위 정규검사는 252/252 정상, 대기·문제·중복·공개 DB 일정 불일치 0건이다. 활성 큐, 최근 문제 작업, 관리자/고객 감사 문제도 모두 0건이고 두 세션은 `ready`다.
- 운영 배포 검증: 스키마 변경 전에 `/home/clown313python/rhythmjoy_ops/backups/db/raj-20260812-231316.sql.gz` 전체 백업을 만들었다. 운영 API는 `db-live-queue`, 관리자 경고 0건이며 실제 관리자 브라우저의 `bootstrap`·`alerts` 요청은 HTTP 200이다. 두 감사 `detail_json`은 모두 `mediumtext`, Cafe24의 웹/API·메일·DB 캐시·감사 타이머·Apache 서비스는 모두 활성 상태다. 미니PC는 기존 Telegram 상태 109개·62,477바이트의 SHA-256을 그대로 영구 경로에 이전했고, 서비스 인자와 배포 파일 해시를 커밋과 대조했다. 재기동 후 23:19 두 실제 달력 검사는 모두 `ready · 인증 쿠키 유지`, 새 Telegram 발송·주기 오류는 0건이다.

## 2026-08-12 — 최근 10일 원장 감사·취소 재검사·세션 원인 이력

- 판정 범위: 오래된 Google 이관·백필 기록은 이번 신뢰도 판정에서 제외하고 `2026-08-02 00:00 KST` 이후 실제 예약 메일과 그 메일에서 생긴 DB 원장·작업만 별도 대조했다. 운영 중인 전체 공개 DB 원장의 중복 여부는 안전망으로 함께 검사하되, 오래된 종결 작업을 최근 누락으로 합산하지 않는다.
- 실제 이메일 원장 대조: Naver IMAP의 10개 운영 폴더를 `readonly=True`, `BODY.PEEK[]`로 읽었다. 원문 202건, 조회 실패 0건, 미확인 메일 0건이며 202건의 `mail_key`가 모두 DB 이메일 이벤트에 있었다. 같은 기간 DB 이벤트는 203건인데 추가 1건은 메일 누락이 아니라 과거 복구 때 근거를 남긴 `Manual` 취소 이벤트다. 원문·파싱 JSON 누락과 파싱 실패는 모두 0건이다.
- 해소된 실패와 현재 실패 분리: 8월 4일 입금대기 메일 1건은 구버전 오류 상태가 남아 있으나 3분 뒤 같은 예약의 결제완료 메일이 정상 반영된 후속 이벤트다. 후속 정상 이벤트가 없는 현재 유효 파싱·처리 문제는 0건이다. 과거 실패 문자열만 보고 현재 누락으로 판정하지 않는다.
- 최근 작업 대조: 실제 메일에서 생긴 플랫폼 작업 193건은 `upload 134`, `naver_block 38`, `delete 16`, `naver_restore 5`이며 전부 `done`이다. 최근 범위의 `pending/running/failed/needs_review`는 0건이다. 문자 발송 의무가 있는데 Outbox가 없는 작업 0건, 최근 문자 문제 상태 0건, 실제 발송 완료 168건이다.
- 작업 증거 저장 오류: 작업 결과 칼럼은 65,535바이트를 담는 MySQL `TEXT`인데 감시기만 임의로 4,000문자로 제한했다. 최근 10일 203건의 실제 결과는 평균 2,186바이트·최대 4,459바이트였고, `#571` Naver 복원 결과는 제한을 넘으면서 최종 상태만 남은 406바이트 축약본으로 교체돼 시간칸별 전후 증거가 사라졌다. 저장 한도를 안전 여유가 있는 48KiB 바이트 기준으로 바꾸고, Naver 차단·복원은 각 시간칸의 날짜·시간·변경 전 상태·변경 후 상태·목표 상태·검증 여부를 작은 구조로 먼저 정규화한다. 핵심 증거조차 한도에 들지 않으면 성공 요약을 저장하지 않고 작업 갱신 자체를 실패시켜 재검사 대상으로 남긴다. 이미 사라진 `#571`의 과거 DOM 전문은 복원할 수 없으므로 현재 실제 Naver 시간칸을 독립 감사해 상태를 새 증거로 남긴다.
- DB·공개 일정 감사: 최근 10일의 반영검사 대상 이메일 180건은 수집·원장·작업 연결 누락 0건이다. 현재 유효 원장 400건도 400건 정상이며 대기·문제·중복·공개 일정 불일치·이관 미완료가 모두 0건이다. 공개 일정 캐시의 원천은 `db-booking-ledger`다.
- 취소 재검사 보강: 기존 고객 실제 플랫폼 순환검사는 확정 예약만 다시 읽고 취소 예약은 당시 작업 결과만 믿는 빈틈이 있었다. 최근 10일 안에 취소됐고 사용 시간이 아직 지나지 않은 고객 예약도 후보에 포함한다. Naver 원본 취소는 SpaceCloud 복제본 부재를, SpaceCloud 원본 취소는 Naver 시간칸의 `예약 가능` 복원을 실제 화면에서 다시 읽는다. 화면 조회 실패는 누락으로 단정하지 않고 3분 뒤 2차 검사하며, 실제 잔존·미복원일 때만 불일치로 판정한다.
- 취소 구간 중첩 오판 방지: 첫 전수검사에서 15건 중 11건은 즉시 일치했고 4건의 일부 시간칸이 `confirmed/suspended`라 불일치로 표시됐으나, 네 건 모두 같은 시간의 다른 유효 예약이 원인이었다. 8월 14일 E홀 20~21시는 별도 Naver 확정, 8월 19일·26일 B홀 8~9시와 9월 17일 E홀 20~22시는 뒤이어 확정된 SpaceCloud 예약의 정상 차단이었다. 따라서 취소 범위 전체를 무조건 `available`로 요구하지 않는다. 시간칸마다 DB의 다른 활성 예약을 겹침 계산하고, Naver 원본 예약은 `confirmed/soldout`, 완료된 Naver 차단 작업을 가진 SpaceCloud·관리자 예약은 `suspended`, 겹침이 없는 칸은 `available`만 허용한다. 맞닿기만 한 예약과 차단 작업이 없는 과거 원본 행은 정상 차단 근거로 인정하지 않는다.
- 취소 실제 화면 최종검사: 위 중첩 규칙을 반영한 뒤 같은 최근 미래 취소 15건을 실제 Naver·SpaceCloud 화면에서 다시 전수 확인했다. `ok 15`, 불일치 0, 재검사 대기 0, 화면 조회 실패 0이다. 중첩이 없는 취소 시간칸은 모두 복원됐고, 겹친 시간칸 네 곳은 각각 다른 활성 예약과 그 예약의 완료된 차단 작업까지 일치했다.
- 8월 12일 Naver 로그인 해제 원인 정정: 장애 전에 쿠키 상태를 수집하지 않았으므로 정확한 원인은 확정할 수 없다. 확인된 사실은 8월 11일 재부팅 뒤에도 8월 12일 18:04:58까지 세션 검사가 정상이었고, 18:08:13에 처음 로그인 화면으로 전환됐다는 점이다. 따라서 재부팅을 직접 원인 또는 단순한 30일 만료로 단정하지 않는다.
- 다음 장애 진단: 매 3분 실제 달력 검사 전·후에 대표 인증 쿠키의 존재 여부·표시 만료시각·HMAC 지문, 부팅 ID·uptime, 자동화 프로필/Cookie DB 식별자, 네트워크 HMAC 지문, 시스템 시각 보정을 기록한다. 원문 쿠키·비밀번호·OAuth 쿼리값·IP 주소는 저장하지 않는다. 로컬 `session-diagnostics.jsonl`을 먼저 기록하고 DB에는 상태 변화 또는 24시간 정상 heartbeat만 보존한다.
- 원인 분류 원칙: 표시 만료시각 도달, 만료 전 로컬 소실, 재부팅 전후 소실, 프로필 저장소 변경, 플랫폼 응답 중 삭제, 표시 만료 전 서버 거부, 브라우저 검사 실패를 분리한다. 표시 만료시각이 없으면 `만료 전`이라고 추정하지 않고 근거 부족으로 기록한다. 관리자 화면은 실제 미니PC 달력 검사만 `ready`로 표시하며 수동 정상 버튼으로 상태를 덮어쓸 수 없다.
- 자동복구 경계: 플랫폼 인증 자체와 2차 인증은 우회하지 않는다. 로그인 해제 중에도 이메일→DB 원장과 Outbox는 계속 저장되고 해당 플랫폼 작업은 시도 횟수를 소모하지 않은 채 대기한다. 사용자가 한 번 재로그인해 실제 두 달력 검사를 통과하면 DB에 남은 미완료 작업을 오래된 순서로 자동 재개한다. Telegram은 로그인 해제와 복구처럼 실제 상태가 바뀔 때만 한 건을 보내며, 표시 만료시각만으로 선제 경고하지 않는다.
- 중단 중 Outbox 자동재개 실증: 실제 예약 작업 `#605`가 취소 전수검사를 위해 워처를 잠시 정지한 동안 DB에 먼저 생성됐다. 워처 재시작 뒤 별도 수동 재처리 없이 오래된 작업으로 자동 선택되어 완료됐고, 새 48KiB 저장 경로에 제출 여부·신원 확인·최종 검증을 포함한 2,001바이트 결과가 보존됐다. 이후 활성 미완료 작업은 0건이다.
- 운영 반영 확인: 배포 후 관리자 API를 실제 화면과 같은 인증 `POST`로 호출해 `ok=true`, 관리자 경고 0건, 세션 진단 이력 2건을 확인했다. Naver와 SpaceCloud 실제 달력 검사는 모두 로그인 유지 상태였고 감시 서비스 재가동 뒤 주기 검사도 정상 종료했다.

## 2026-08-12 — 미니PC 원격 복구 및 플랫폼 로그인 세션 절차

- 상태: 원격 접속 경로 구성 완료. 맥북의 Windows App은 `Rhythmjoy Mini PC` 장치로 `127.0.0.1:13389`에 접속하며, macOS LaunchAgent `com.rhythmjoy.mini-rdp-tunnel`이 Cafe24의 `127.0.0.1:22013` 역방향 SSH를 거쳐 미니PC RDP `127.0.0.1:3389`로 연결한다. 터널은 로그인·재부팅·네트워크 단절 뒤 자동 재시작한다.
- `0x204` 판정법: 미니PC의 `gnome-remote-desktop.service`, 3389 리슨, `rhythmjoy-reverse-ssh.service`가 정상인데 맥북 `127.0.0.1:13389`만 거부되면 RDP/암호 오류가 아니라 맥북 로컬 터널 종료다. `ops/open-ubuntu-remote-desktop.sh`가 LaunchAgent를 다시 설치·기동하고 실제 포트가 열린 뒤 Windows App을 연다. `0x207`과 서버의 NLA 인증 실패는 터널 장애와 구분하며, 키체인 값을 미니PC RDP 자격 증명과 다시 맞춘 뒤 암호 끝 줄바꿈 없이 클립보드에 복사한다.
- 보안: RDP 계정은 `rhythmjoy-remote`, 암호는 macOS 키체인의 `Rhythmjoy Mini PC RDP` 항목과 미니PC GNOME Remote Desktop 자격 증명에만 저장한다. 저장소와 로그에는 암호를 기록하지 않는다. Windows App의 오디오·마이크·카메라·프린터·폴더·스마트카드·클립보드 리디렉션은 비활성화한다. Codex에는 맥북 화면·오디오·손쉬운 사용 권한을 부여하지 않는다.
- 플랫폼 재로그인: 임의 Chrome이나 여러 탭을 열지 않고 미니PC 화면 `:0`의 전용 프로필 `/home/kiosk-j/.spacecloud-automation`에서 `node tools/spacecloud-watch.mjs login`만 사용한다. 네이버 OAuth URL(`nid.naver.com`) 또는 파트너 인증 URL(`partner.spacecloud.kr/auth/`)에 들어간 동안 자동 새로고침·재이동을 금지해 OAuth 토큰을 보존한다.
- 복구 순서: 워처와 kiosk Chrome을 중지한 뒤 로그인 도우미 한 개만 실행한다. 스페이스클라우드에서 실제 `예약추가`가 보일 때까지 기다리고, 이어 같은 전용 프로필에서 네이버 SmartPlace 달력까지 확인한다. 두 플랫폼 독립 세션 검사가 모두 `ready`인 경우에만 로그인 창을 정상 종료하고 워처를 다시 시작한다. 실패하면 워처를 켜지 않고 세션 장애 상태를 유지한다.
- 실제 복구 검증: 2026-08-12 19:48 KST에 순차 로그인 도우미가 스페이스클라우드 달력과 네이버 SmartPlace 달력을 모두 확인한 뒤 프로필을 정상 종료했다. 이어 구버전과 독립된 `check-login`, `check-naver-login`을 각각 실행해 두 결과가 모두 `ok=true`임을 확인했다. 워처와 kiosk Chrome을 재가동한 첫 주기에서 세션 오류 없이 등록 작업 `#601`과 삭제 작업 `#602`가 완료됐고, 상태 변경 Telegram도 각각 한 건만 발송됐다.
- 미완료 복구 상태기계: 예약 이메일을 DB 원장·플랫폼 Outbox에 저장하는 같은 트랜잭션에서 예약 작업의 `confirmation_sms_required=1` 의무 표시와 문자 후속작업 `pending` 행을 외부 브라우저 작업보다 먼저 함께 기록한다. 둘 중 하나라도 저장에 실패하면 예약 handoff 전체를 롤백하고 이메일을 미처리로 남긴다. 별도 감시 루프도 매 순환마다 `문자 의무가 있는 작업 ↔ 문자 후속작업`을 대조해 후속 행이 사라졌다면 `INSERT IGNORE`로 자동 복원한다. 따라서 프로세스·브라우저·세션이 작업 직전에 죽어도 의도가 사라지지 않는다. 플랫폼 작업과 문자 후속작업은 실패 원인·시도 횟수·마지막 시도·다음 재시도 시각을 DB에 남기며, 세션 장애 동안 해당 플랫폼 작업은 시도 횟수를 소모하지 않고 대기한다. 세션이 `ready`로 바뀌면 오래된 미완료부터 5분→15분→60분(장기 실패는 6시간) 간격으로 자동 재실행하고 실제 반영/발송이 확인돼야 완료한다. 3회 이상 계속 실패하는 예약만 상태 변화 시 Telegram 한 건을 보내고, 해결된 경우에만 복구 한 건을 보낸다. 문자 업체가 발송을 받았는지 불확실한 `uncertain` 상태는 중복 발송 방지를 위해 자동 재발송하지 않는다. 15분 넘게 `pending`인 후속작업과 5분 넘게 `sending`인 후속작업은 관리자 경고 센터에도 표시한다.
- 다음 만료 대응: 맥북에서 `ops/recover-ubuntu-platform-sessions.sh`를 실행한다. 이 명령이 RDP 터널과 Windows App을 열고 미니PC 워처·키오스크를 정지한 뒤 전용 프로필 로그인 도우미 하나만 실행한다. 사용자는 2차 인증만 완료하면 된다. 도우미가 두 플랫폼 달력을 확인하고 독립 재검사까지 통과한 경우에만 워처·키오스크를 자동 재시작한다.
- 관련 파일: `ops/com.rhythmjoy.mini-rdp-tunnel.plist`, `ops/open-ubuntu-remote-desktop.sh`, `ops/recover-ubuntu-platform-sessions.sh`, `ops/ubuntu-platform-session-recovery.sh`, `ops/rhythmjoy-platform-session-recovery.service`, `tools/spacecloud-watch.mjs`

## 2026-08-12 — 동기화 감사 오판·이벤트 역전·반복 실행 구조 개선

- 상태: 해결 및 운영 반영 완료.
- 감사 범위: 고객 예약 정기검사는 원본 플랫폼을 항상 확인하되, 반대 플랫폼은 해당 예약의 복제 작업이 `done` 또는 과거 `google_pending`으로 완료된 기록이 있을 때만 확인한다. 과거 플랫폼 내보내기처럼 복제를 만든 적 없는 예약을 반대편 누락으로 판정하지 않는다.
- 판정 설명: Telegram에는 `원본만` 또는 `원본 + 완료 기록이 있는 복제본` 검사 범위를 명시한다. 고객·관리자 일정 모두 화면 조회 실패는 첫 회 알림 없이 재검사 대기로 두고 3분 뒤 같은 예약을 다시 읽으며, 연속 실패 시에만 확인 필요 알림을 보낸다. 첫 실패 뒤 정상 확인된 경우에는 발송한 문제가 없으므로 복구 알림도 보내지 않는다.
- DB 시간 순서: 지연 도착한 오래된 확정·취소 이메일이 최신 원장의 날짜·홀·시간·이름·금액·payload를 덮지 못하도록 현재 상태뿐 아니라 모든 표시 필드를 조건부 갱신한다. 초 단위 수신시각만으로 순서를 추정하지 않고, 네이버 `Message-ID`의 검증된 source millisecond를 `event_order_key`로 영속화해 모든 메일함을 함께 정렬하며 원장은 `last_event_order_key`를 사용한다. source 시계가 없거나 수신시각과 비정상적으로 어긋나면 수신시각 fallback key는 감사·정렬용으로만 저장하고 `event_order_trusted=0`으로 격리해 자동 등록·삭제를 금지한다. 같은 예약의 확정·취소가 source millisecond까지 같아 순서를 증명할 수 없는 경우에도 DB 저장 순서로 추정하지 않고 격리한다. 관리자 자동·수동취소보다 오래된 확정 이메일도 취소 상태를 되돌릴 수 없다.
- 단일 감사 구현: Ubuntu 감시기에 복제돼 있던 내장 Python 감사 코드를 제거하고 Cafe24의 `rhythmjoy_reflection_audit.py`만 호출한다. 서버 타이머와 수동 검사가 같은 코드·같은 판정식을 사용한다.
- 실행·로그 정책: Cafe24 감사 타이머를 문서와 동일한 30분으로 조정한다. Ubuntu 워처 표준 출력은 용량 제한이 있는 systemd journal로 전환하고, `runs.jsonl`은 16MB 기준 4개 보관으로 순환한다. Cafe24 캐시·감사 로그는 20MB 기준 8개 압축 보관한다.
- 배포 중 오경고 검증: 2026-08-17 D홀 06:00-07:00 취소 건의 `Target page, context or browser has been closed` 알림은 실제 누락이 아니라 워처 배포 재시작과 겹친 첫 조회 중단이었다. 재검사에서 DB `canceled`, 스페이스클라우드 일정 없음, 네이버 전 시간칸 `예약 가능`이 일치했고 감사 상태는 `ok`로 복구됐다. 이제 서비스 종료로 중단된 순환은 알림을 생략하며, 일반 조회 실패도 첫 회는 조용히 3분 재검사하고 2회 연속 실패할 때만 알린다.
- 최종 검증: 실제 관리자 미래 예약 2건은 두 플랫폼 4개 확인 모두 정상, 스페이스클라우드 원본 전용 고객 예약은 불필요한 네이버 복제 검사 없이 정상으로 확인됐다. DB 기반 전체 감사 394/394, 수집 감사 99/99, 문제·대기·중복·공개 일정 불일치·수집 누락·활성 작업이 모두 0건이다. 실제 MariaDB 임시 테이블에서 지연 확정·지연 취소·같은 초 확정/취소 순서 테스트도 통과했으며 운영 원장 행은 시험에 사용하지 않았다.
- 관련 커밋: `b8bf3a0`, `ba61e44`, `e27e134`

## 2026-08-12 — 8월 12일 A홀 19–21시 예약 변경 오판 및 감사 범위 보강

- 상태: 일정 누락 아님. 오경고 해제 및 판정 로직 수정 완료.
- 실제 변경 흐름: 기존 A홀 19:00-20:00 네이버 예약 `1285497055`는 유지됐다. 기존 20:00-22:00 예약 `1101015533`은 8월 3일 13:20:15 취소됐고, 40초 뒤 20:00-21:00 새 예약 `1311473340`이 확정됐다.
- 교차 증거: 스페이스클라우드 삭제 작업 `#413`은 20:00-22:00만 식별해 삭제한 뒤 19:00-20:00 일정이 남았음을 확인했다. 등록 작업 `#414`의 사전 검사에는 19:00-20:00과 20:00-21:00 두 일정이 함께 보였다. 현재 DB 원장과 공개 캐시도 두 구간을 각각 `ledger:131`, `ledger:9027`로 표시한다.
- 오판 원인: 새 실제 플랫폼 감사가 예약 변경 묶음과 인접 대체 예약을 보지 않고 `1285497055` 한 건만 네이버 목록에서 검색했다. 목록 검색의 `not_found`는 취소 목록 필터, 화면 로딩, 검색 조건 등으로도 발생할 수 있는데 이를 즉시 실제 누락으로 승격한 분류가 잘못이었다. 두 번째 스페이스클라우드 확인도 수동 검사 중 브라우저 종료로 완료되지 않았으므로 당시 불일치 판정은 유효하지 않았다.
- DB 정리: `ledger:131`은 과거 내보내기의 잘못된 취소 판정을 8월 1일 실제 확정으로 복원한 행이지만 `payment_status=예약취소`가 남아 있었다. 복원 근거 메모와 예약번호를 조건으로 결제상태만 `결제완료`로 정정했으며 일정·예약번호·금액은 바꾸지 않았다. 시험 중 생성된 고객 플랫폼 감사 경고는 `resolved`로 해제했다.
- 재발 방지: 네이버 목록에서 예약번호를 못 찾은 결과는 더 이상 누락·취소로 판정하지 않는다. 예약 상세에서 상태가 명시적으로 `취소`인 경우에만 DB 불일치로 확정한다. 화면 판독 실패는 첫 회 `recheck_pending`으로만 저장하고 같은 건이 다음 검사에서도 실패해야 관리자 확인 대상으로 올린다.
- 봇·세션 정책: 자동 로그인은 하지 않고 전용 브라우저 프로필의 기존 세션만 재사용한다. 로컬 고객 예약 순환검사는 기본 비활성화했다. 운영 서버에서만 Xvfb 화면으로 4시간마다 1건을 검사하며, 세션이 만료되면 재로그인 시도 없이 검사 실패로 중단한다.
- 전체 중복 감사: 기존 이메일 생성 원장끼리만 보던 검사를 모든 활성 DB 원장과 관리자 일정을 합친 검사로 확장했다. 정확 중복뿐 아니라 부분 시간 겹침, 자정을 넘는 겹침, 원장-관리자 일정 겹침도 탐지하며 맞닿은 시간은 충돌로 보지 않는다.

## 2026-08-12 — 8월 17일 A홀 구글 종료 이관 누락

- 상태: 해결 및 운영 반영 완료.
- 대상: 스페이스클라우드 예약 `10222302`, 2026-08-17 A홀 15:00-19:00, 용태은. 실제 스페이스클라우드 예약과 네이버 예약불가는 있었지만 공개 일정표에서만 빠졌다.
- 원인: 6월 29일 예약은 현재 DB 원장 도입 전 자료였고, 7월 14일 실제 플랫폼 전수집 때 DB 138번으로 복구됐지만 기존 `google-backfill` 분류가 보존됐다. 8월 10일 공개 일정표를 DB 전용으로 바꾸면서 이 분류가 표시 대상에서 제외됐다. DB 행이 삭제된 것이 아니라, 실제 플랫폼 원장으로 승격하지 않은 이관 누락이었다.
- 조치: DB 138번의 기존값을 복구용 JSON으로 백업한 뒤, 예약번호·날짜·홀·시간·실제 플랫폼 payload가 모두 일치할 때만 `spacecloud` 원장으로 승격했다. 네이버 차단은 이미 있어 재동기화하지 않았다.
- 재발 방지: 실제 플랫폼 전수집이 `google-backfill`과 매칭되면 원천을 `naver` 또는 `spacecloud`로 승격하도록 수정했다. 승격 전에도 `visible-site-year-backfill` 검증 행은 공개 캐시에 표시하며, 미래 확정 `google-backfill`이 하나라도 남으면 정합성 검사가 원장 이관 문제로 경고한다.
- 검증: 공개 DB 캐시에 `ledger:138` / `spacecloud` / 2026-08-17 A홀 15:00-19:00 / 예약번호 `10222302`가 표시된다. 미래 확정 `google-backfill` 0건, 정합성 검사 395/395 정상, 수집 누락 0건, 공개 일정 불일치 0건, 이관 미완료 0건을 확인했다.
- 관련 커밋: `75e4f9b`

## 2026-08-10 — 관리자 예약 실제 플랫폼 정기 대조

- 상태: 해결 및 운영 반영 완료.
- 감시기가 DB 작업의 `done` 표시만 신뢰하지 않고, 미래 관리자 예약을 DB에서 읽은 뒤 네이버 예약 가능/불가 시간칸과 스페이스클라우드 직접등록 일정의 존재·식별정보를 브라우저로 다시 읽는다.
- 30분마다 미검사 또는 가장 오래전에 검사한 예약 2건을 순환 선택하고 예약당 두 플랫폼을 확인한다. 운영 자동화와 같은 브라우저 문맥을 사용하므로 별도 검사 프로세스가 자동화 Chrome 프로필을 잠그지 않는다.
- `플랫폼 불일치`와 `화면을 읽지 못한 검사 실패`를 구분한다. 검사 실패는 누락으로 확정하지 않고 다음 순환에서 다시 검사하며, Telegram은 예약별 상태가 새로 바뀔 때만 한 건 발송하고 정상 상태를 매번 알리지 않는다. 이전 문제가 정상으로 돌아온 경우에만 복구 메시지 한 건을 보낸다.
- 운영 상태는 `admin-platform-audit-state.json`에 예약별 마지막 확인 시각, 판정, 두 플랫폼 결과를 기록한다. 한 번만 강제 점검할 때는 `node tools/spacecloud-watch.mjs admin-platform-audit --json`을 사용한다.
- 실제 정기대관 E2E: 2026-08-12·14·17 D홀 06:00-07:00 세 건을 등록해 DB `confirmed`, 스페이스클라우드 등록, 네이버 예약불가를 확인했다. 8월 14일 한 건 부분 취소 후 나머지 두 건 유지, 이어 미래 전체 취소 후 세 건 모두 DB `canceled`, 공개 캐시 제거를 확인했다. 독립 읽기 재검사에서도 스페이스클라우드 3/3 부재, 네이버 3/3 예약 가능 복원으로 총 6/6가 일치했다.
- 관련 파일: `tools/spacecloud-watch.mjs`, `ops/install-ubuntu-spacecloud-watch.sh`, `ops/install-spacecloud-watch.sh`

## 2026-08-10 — 관리자 정기대관·다건 취소 중앙제어

- 상태: 해결 및 운영 반영 완료.
- 관리자 입력은 기존 단건 등록과 별도로 `정기대관 등록`을 제공한다. 시작일·종료일, 요일별 홀/시간 복수 규칙, 5주차 포함/제외를 설정하면 서버가 최대 366일·500건을 정확한 날짜 목록으로 계산한다.
- 등록 전 12개월 미니 달력에서 DB 원장과 관리자 일정의 충돌을 빨간색으로 표시한다. 5주차 제외는 노란색, 날짜별 홀·시간·포함 여부를 바꾼 예외는 보라색으로 표시하며 변경 후 서버가 다시 충돌을 검사한다.
- 충돌 날짜 하나만 다른 홀/시간/날짜로 바꾸거나 제외할 수 있고, 전체 반복 규칙은 그대로 유지한다. 5주차 기본 제외 후 특정 5주차만 다시 포함하는 것도 같은 예외 처리로 지원한다.
- 등록 확정 시 정기대관 묶음, 각 날짜 관리자 예약, DB 원장 앵커, 네이버 예약불가/스페이스클라우드 등록 작업을 한 DB 트랜잭션에서 생성한다. 같은 요청 ID는 중복 등록하지 않는다.
- 취소는 오늘 이후 일정 하나, 선택한 여러 날짜, 남은 일정 전체를 지원한다. DB 행을 삭제하지 않고 `canceling`으로 유지하면서 스페이스클라우드 삭제와 네이버 복구를 실행하고, 두 작업이 완료된 뒤에만 `canceled`로 확정해 공개 일정에서 제거한다.
- 2026-08-10 보완: 정기대관 전체 취소의 범위를 `오늘을 포함한 남은 일정 전체`로 제한했다. 지난 일정은 UI에서 선택할 수 없고 API에서도 날짜 조건으로 거부하여 운영 이력을 보존한다.
- 관리자 작업은 이메일에서 생성되지 않으므로 감시기가 이메일 이벤트 ID를 요구하지 않고 `admin-task-anchor`의 현재 상태만 검증하도록 수정했다. 각 등록/취소 작업이 끝날 때 관리자 예약과 정기대관 묶음 상태를 자동 확정한다.
- 검증: PHP 5.4 문법 검사, 정기 규칙/5주차/날짜별 예외 자체검사, Node 문법 검사, 감시기 자체검사 통과. 브라우저 더미 DB 테스트에서 2026-09-01~2027-08-31 월·금 규칙 104건·12개월 표시, 5주차 제외 8건, 특정 5주차 재포함 후 제외 7건/부분변경 1건, 충돌 날짜 홀 변경 후 충돌 0건, 선택 취소 2건 선택을 확인했다.
- 관련 파일: `www/sync-admin/index.html`, `www/sync-admin/app.js`, `www/sync-admin/styles.css`, `www/sync-admin/api.php`, `tools/spacecloud-watch.mjs`, `tools/sync-admin-ui-mock.mjs`, `docs/sync-admin-recurring-runbook.md`

## 2026-08-10 — DB 원장 전환 및 외부 캘린더 연동 종료

- 상태: 해결 및 운영 반영 완료.
- 현재 운영 순서: `예약 이메일 원문 → DB 이메일 이벤트/예약 원장/반대 플랫폼 작업 트랜잭션 저장 → 반대 플랫폼 실제 반영 및 검증 → DB 작업 결과 확정 → DB 공개 일정 캐시 갱신 → 정합성 검사`.
- 공개 일정표는 `rhythmjoy_booking_ledger`와 `rhythmjoy_admin_reservations`를 합친 DB 캐시만 읽는다. 브라우저의 외부 캘린더 동기화 스크립트와 공개 API 키·캘린더 ID는 제거했다.
- 이메일 수집기와 Ubuntu 감시기의 외부 캘린더 생성·삭제·재시도 코드는 제거했다. 기존 외부 캘린더 데이터는 삭제하지 않고 과거 자료로 보존한다.
- Telegram은 예약별 최종 상태가 바뀔 때만 알리고, Cafe24 정합성 검사는 30분마다 실행하며 문제가 없으면 발송하지 않는다.
- 과거 확인 필요 작업 `#485`, `#508`, `#513`, `#514`, `#515`는 실제 플랫폼 재검증 후 정리됐다. `#513`의 8월 12일 C홀 19:00-20:00도 네이버 반영과 DB 공개 일정 표시를 확인했다.
- 2026-08-09 항목은 수정 전 조사 당시의 판단과 수치를 보존한 역사 기록이다. 현재 운영 절차로 사용하지 않는다.

## 2026-08-09 — 최근 6일 예약 동기화 재검토 인수인계

- 상태: 조사 중 / 수정 전. 이 기록을 남긴 시점까지 코드, DB 행, 작업 상태, 브라우저 플랫폼 일정, 서비스는 변경하거나 재처리하지 않았다.
- 조사 기준: KST `2026-08-04 00:00:00` 이상 `2026-08-10 00:00:00` 미만에 수신된 네이버·스페이스클라우드 예약/취소 이메일을 대상으로 했다.
- 확정 운영 순서: `예약 이메일 원문 → DB 이메일 이벤트 저장 → DB 예약 원장과 반대 플랫폼 작업을 트랜잭션으로 생성 → 반대 플랫폼 실제 반영 및 검증 → DB 작업 결과 확정 → Google 최종 일정 반영 → 전체 정합성 검사`. 이메일 원장과 DB는 별도 시스템이 아니라, 이메일이 원천이며 이메일 이벤트 원장은 DB에 보존된다. Google Calendar는 예약 원천이 아닌 최종 기록 계층이다.
- 1차 DB/로그 집계: 대상 이메일 이벤트는 총 72건이며 모두 파싱됐다. 네이버 예약 56건 중 52건은 `upload/done`, 4건은 `upload/needs_review`; 스페이스클라우드 예약 11건 중 10건은 `naver_block/done`, 1건은 `naver_block/needs_review`; 네이버 취소 4건은 모두 `delete/done`; 스페이스클라우드 취소 1건은 `naver_restore/done`이다. 따라서 DB 작업 상태 기준 완료 67건, 확인 필요 5건이지만, 이 수치만으로 실제 플랫폼 동기화 성공률을 확정하지 않는다.
- 현재 정규검사의 9개 문제 표시는 서로 다른 예약 9건이 아니다. 반대 플랫폼 `needs_review` 5건과 그중 Google 최종 일정이 없는 4건을 중복 집계한 결과로, 고유 예약은 5건이다. 메일 앞단은 115건 점검/누락 0건, 확정 중복 0건이었다.
- 확인 필요 작업:
  - `#485` 네이버→스페이스클라우드, `2026-08-07 B홀 14:00-16:00`, 예약번호 `1315144501`: 같은 시간·이름의 스페이스클라우드 후보는 보였으나 상세 신원 검증이 오류로 끝나 `needs_review`가 남았다. Google 최종 일정에는 같은 예약번호·슬롯이 실제 존재한다. 과거 작업 상태가 남은 경우인지 재확인이 필요하다.
  - `#508` 네이버→스페이스클라우드, `2026-08-09 C홀 13:00-17:00`, 예약번호 `1317110201`: 제출 후 같은 시간·이름의 후보가 보였으나 팝업에서 네이버 예약번호를 확인하지 못해 `needs_review`가 됐다. 실제 생성 후 검증만 실패한 가능성이 높지만 확정하지 않았다. Google 최종 일정은 없다.
  - `#513` 스페이스클라우드→네이버, `2026-08-12 C홀 19:00-20:00`: 네이버 슬롯은 반영 전 `예약가능`이었고 상세 패널이 방·날짜·시간 내용을 제공하지 않아 저장 전에 중단됐다. 로그 기준 네이버 예약불가 반영은 완료되지 않았으며 Google 최종 일정도 없다.
  - `#514` 네이버→스페이스클라우드, `2026-12-04 A홀 21:00-23:00`, 예약번호 `1317631604`: 제출 후 같은 날짜 셀에 같은 시간·이름 후보가 보였으나 팝업 날짜·예약번호 검증이 실패했다. 실제 생성 후 검증만 실패한 가능성이 높지만 확정하지 않았다. Google 최종 일정은 없다.
  - `#515` 네이버→스페이스클라우드, `2026-08-10 E홀 19:00-20:00`, 예약번호 `1317724211`: 첫 시도에서 SpaceCloud 세션 확인이 `login_required`로 기록되고 타임아웃이 발생했다. 두 번째 시도도 모달이 닫히지 않았으며 약 107초의 제출 후 확인에서도 후보 0건이라 실제 일정 생성 실패로 판단된다. Google 최종 일정도 없다.
- Google 상태: 공개 최종 캐시는 정상 갱신 중이고 방별 조회 실패는 0건이었다. 최근 Google API 인증·호출 실패 로그는 발견되지 않았다. 위 4건의 Google 누락은 Google API 오류로 확인된 것이 아니라, 반대 플랫폼 작업이 `needs_review`에서 종료돼 안전 순서상 Google 생성 단계에 진입하지 않은 결과다.
- 운영 상태: Cafe24 이메일 서비스·Google 캐시·정규검사 타이머와 Ubuntu 미니PC의 워처·Chrome·역방향 SSH 서비스가 활성 상태였다. 마지막 세션 검사에서 Naver와 SpaceCloud 모두 `ready`였다. Ubuntu 워처는 운영 커밋 `f35e9db`였고, 이후 두 커밋은 입금대기 이메일 처리 변경으로 이번 브라우저 검증 문제와 직접 관련되지 않는다.
- 별도 알림 문제: Cafe24 정규검사는 5분마다 실행된다. 동일 이슈를 쿨다운으로 억제한 실행에서 다음 상태 파일에 `lastSentAtEpoch`를 보존하지 않아, 같은 메시지가 실질적으로 10분마다 다시 발송된다. Ubuntu 워처도 별도 30분 정규검사 알림을 보내므로 같은 시각에 제목이 같은 알림이 중복될 수 있다. 이는 예약 동기화 자체와 분리해서 수정해야 한다.
- 후속 검증 원칙: 다음 작업에서는 `done` 상태를 성공의 증거로 그대로 신뢰하지 말고 최근 6일의 예약 확정 67건을 원천 이메일/DB 원장 기준으로 다시 잡아, 네이버→스페이스클라우드는 실제 일정 존재와 식별정보, 스페이스클라우드→네이버는 실제 예약불가 슬롯, 이후 Google 최종 일정까지 순서대로 읽기 검증해야 한다. 취소 5건도 상대 플랫폼 제거·복구와 Google 최종 부재를 별도로 확인한다. `#508`·`#514`는 중복 생성 위험 때문에 실제 일정 존재 확인 전에 재등록하면 안 된다.
- 관련 파일: `ops/rhythmjoy_email_import.py`, `tools/spacecloud-watch.mjs`, `tools/spacecloud-playwright-uploader.mjs`, `tools/naver-playwright-availability.mjs`, `ops/rhythmjoy_reflection_audit.py`, `ops/rhythmjoy-reflection-audit.timer`

## 2026-08-04 — 입금대기 예약메일 반복 실패 알림

- 상태: 해결
- 현상: 네이버 `입금대기` 예약메일 한 건이 약 1분마다 재처리되며 Telegram에 `Required SpaceCloud upload task was not created` 오류를 반복 전송했다.
- 원인: 결제완료 예약만 SpaceCloud 업로드 대상으로 삼는 기존 정책과, 활성화된 업로드 경로에서 모든 예약메일에 필수 Outbox 생성을 요구하는 트랜잭션 검증이 충돌했다. `입금대기`는 의도대로 업로드 작업 생성을 건너뛰었지만 필수 handoff 실패로 오인되어 메일이 읽음 처리되지 않았다.
- 해결: 결제완료 전 예약메일을 `reservation_pending` / `payment_pending`으로 보존하고 읽음 처리하며, 예약 원장과 SpaceCloud Outbox는 만들지 않도록 분기했다. 결제완료 후 도착하는 별도 메일은 기존 원장·Outbox 트랜잭션 경로를 그대로 사용한다. 과거 자료 백필도 결제대기 예약을 확정 원장으로 만들지 않도록 제외했다.
- 검증: 로컬 및 운영 Python 3.8 환경에서 이메일 파이프라인 자체 테스트 11개가 통과했다. 운영 재처리에서 입금대기 메일은 `payment_pending`으로 종료됐고, 뒤이어 도착한 결제완료 메일은 SpaceCloud 작업 `#462`를 생성했다. 최종 반영 감사는 295/295 정상, 문제 0건, 대기 0건, 중복 0건이었으며 이후 수집 주기 2회도 처리 0건·오류 없이 종료됐다.
- 관련 파일: `ops/rhythmjoy_email_import.py`, `ops/rhythmjoy_email_import_selftest.py`
